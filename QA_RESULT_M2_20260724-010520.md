# 全平台便签 M2 验收结果

验收版本：本地工作区 `/Users/dian/Documents/全平台便签`
验收日期：2026-07-24
验收人员：Codex

结论：不通过

覆盖范围：按 `QA_CHECKLIST_M2.md` 执行 QA-M2-001 至 QA-M2-034，共 34 项。

结果汇总：

- 通过：31
- 不通过：3
- 未测：0

## 阻塞问题

1. QA-M2-008 空待办状态不符合预期
   - 复现步骤：新建空便签，切换为待办模式，刷新页面。
   - 实际结果：页面不崩溃，也没有 undefined/null，但刷新后没有显示可编辑空待办项。
   - 期望结果：至少显示一条可编辑空待办。

2. QA-M2-016 删除确认文案不符合预期
   - 复现步骤：创建普通便签，点击删除，确认后进入回收站。
   - 实际结果：删除后主页不再显示，回收站可见；但源码确认普通删除弹窗文案为“确定删除此便签？”，未说明可在回收站恢复。
   - 期望结果：删除确认文案说明可在回收站恢复。

3. QA-M2-025 移动端返回列表状态异常
   - 复现步骤：390px 宽度下打开主页，新建普通便签，点击返回列表，再进入编辑。
   - 实际结果：页面存在两个“返回列表”按钮；点击后列表和编辑内容同时残留，未回到干净列表状态。
   - 期望结果：返回后列表状态正常，点击便签再进入编辑。

## 非阻塞说明

- QA-M2-031 初测时在先执行 `npm run build` 再启动 `next dev` 后，动态 API route 曾出现 Next dev 模块缓存 500：`__webpack_modules__[moduleId] is not a function`。重启 dev server 后复测通过，不计为产品缺陷。
- QA-M2-034 未导入真实旧版数据库；使用仅 `title/content` 的旧形态数据进行兼容模拟，验证 `mode=text`、`checklistItems=[]`、内容不丢。

## 逐项结果

| 编号 | 结果 | 关键证据 |
|---|---|---|
| QA-M2-001 | 通过 | 未登录访问 `/` 跳转 `/login`；登录后进入 `/`；刷新保持登录态。 |
| QA-M2-002 | 通过 | 新建聚焦、显示保存中/已保存；刷新后列表和重新打开内容均存在。 |
| QA-M2-003 | 通过 | 快速连续修改标题和正文后，刷新重新打开保存为最终标题/正文。 |
| QA-M2-004 | 通过 | 三行正文转换为 3 条待办，checkbox 数量为 3。 |
| QA-M2-005 | 通过 | Enter 后新待办聚焦到下一行，刷新后新增项存在。 |
| QA-M2-006 | 通过 | 勾选刷新保持；取消后恢复未完成样式。 |
| QA-M2-007 | 通过 | 删除非最后一条后，其他待办保留且仍可编辑。 |
| QA-M2-008 | 不通过 | 刷新后未显示可编辑空待办项。 |
| QA-M2-009 | 通过 | 反复切换后内容为 `[ ] 买牛奶`, `[x] 已完成`, `[ ] 未完成`，无前缀叠加。 |
| QA-M2-010 | 通过 | checkbox 与输入中心线对齐；完成项有删除线；长文字不遮挡。 |
| QA-M2-011 | 通过 | blue/green/yellow 卡片背景生效；置顶/普通分区清楚。 |
| QA-M2-012 | 通过 | 无标题普通/待办分别使用正文首行/首个待办；空便签显示“无标题便签”。 |
| QA-M2-013 | 通过 | 进度从 `1/3` 更新到 `2/3`，刷新后仍正确。 |
| QA-M2-014 | 通过 | 标题和正文搜索命中并高亮；清空后恢复列表。 |
| QA-M2-015 | 通过 | 待办项搜索命中并高亮；清空后恢复列表。 |
| QA-M2-016 | 不通过 | 行为正确进入回收站，但普通删除确认文案未说明可恢复。 |
| QA-M2-017 | 通过 | 回收站恢复后从回收站消失，主页恢复且内容保持。 |
| QA-M2-018 | 通过 | 永久删除文案包含“永久删除后无法恢复”；删除后搜索不到。 |
| QA-M2-019 | 通过 | 回收站正文关键词搜索命中；清空后恢复列表。 |
| QA-M2-020 | 通过 | 停止服务阻断保存后显示“保存失败”，输入保留，有“重试”。 |
| QA-M2-021 | 通过 | 重启服务点击重试后显示已保存，刷新后内容存在。 |
| QA-M2-022 | 通过 | 保存中切换便签，切回和刷新后原修改不丢失。 |
| QA-M2-023 | 通过 | 标题、正文、待办输入中 Cmd+Enter 均创建新便签并聚焦。 |
| QA-M2-024 | 通过 | 多行文本和 20+ 待办下编辑区可滚动，不遮挡继续输入。 |
| QA-M2-025 | 不通过 | 390px 返回列表后列表和编辑内容同时残留。 |
| QA-M2-026 | 通过 | 390px 下待办新增、勾选、删除、刷新均可用，对齐正常。 |
| QA-M2-027 | 通过 | 390px 下搜索/清空可用，回收站入口和恢复可完成。 |
| QA-M2-028 | 通过 | 无 cookie 访问 floating 返回 307 到 `/login`；自己的便签可打开；不存在 ID 显示 404。 |
| QA-M2-029 | 通过 | 320px floating 无侧边栏，标题/正文/待办/颜色/置顶可编辑且保存正常。 |
| QA-M2-030 | 通过 | 主编辑与 floating 双向修改一致；模式切换无前缀叠加。 |
| QA-M2-031 | 通过 | 非法 checklistItems POST/PATCH 返回 400；GET 始终返回数组。 |
| QA-M2-032 | 通过 | windowX/Y/Width/Height/alwaysOnTop PATCH 后 GET 一致。 |
| QA-M2-033 | 通过 | `npm run build` 通过，无 TypeScript 错误。 |
| QA-M2-034 | 通过 | 旧形态模拟数据返回 `mode=text`、`checklistItems=[]`，内容不丢。 |

## 建议下一步

1. 修复 QA-M2-008：空待办刷新/重新进入时应保证至少一条可编辑空项。
2. 修复 QA-M2-016：普通删除确认文案增加“可在回收站恢复”。
3. 修复 QA-M2-025：移动端返回列表后应隐藏编辑区，并避免重复返回按钮造成状态混乱。
