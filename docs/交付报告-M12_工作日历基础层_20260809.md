# M12 交付报告：工作日历基础层 + Windows 壳外链排查

日期：2026-08-09

## 一、交付内容

按 `docs/SPEC_M12_工作日历基础层_20260809.md` 实现工作日历基础层，管理员灰度上线：

- **结构化数据模型**：新增 `CalendarEvent`（title/date/startTime/endTime/allDay/status/source/noteId 关联/软删 `deletedAt`），`source` 固定 `manual` 为 M13 AI 导入预留。
- **管理员专用 CRUD API**：`GET /api/calendar-events?from&to`、`POST /api/calendar-events`、`PATCH /api/calendar-events/[id]`、`DELETE /api/calendar-events/[id]`。全部 `requireAdmin()`：未登录 401、普通用户 403、只能操作自己的事件、noteId 校验（属于当前用户且未删除）。
- **日历页** `/calendar`（管理员侧边栏入口）：月视图（7 列网格、≤3 事件卡片、+N、今天高亮）、周视图、今日列表（勾选完成/新建/点击事件）。320px 窄窗口降级为日期+圆点，无重叠、无横向溢出。
- **事件编辑弹窗**：标题/日期/全天开关/起止时间/状态/关联便签/备注；便签选择复用便签搜索（标题+正文匹配）。
- **首页今日条**：仅管理员展示——今日日期、未完成数量、前 3 条事件、「查看日历」「+今日」入口；点击事件有关联便签打开便签，否则打开编辑弹窗。
- **普通用户灰度**：无侧边栏入口、无今日条、直接访问 `/calendar` 重定向回首页并提示「无权限访问日历」、日历 API 返回 403。

### Windows 壳外链排查（小排查，时间盒内收口）

按 SPEC 二、2.2 做了小范围排查，结论：

- `src-tauri/src/lib.rs` 已存在 `open_external` 与 `open_external_system` 命令；`permissions/default.toml` 与 `capabilities/default.json` 已含 `allow-open-external`。
- **根因指向旧包**：Windows 桌面壳当前安装的安装包早于 `open_external` 命令合入的提交（`a16999b`），旧包没有该命令 → 前端 `invoke("open_external")` 抛错、静默失败。
- 处置：**记录为桌面壳 P1：重打 Windows 包（含 `a16999b` 后的 `open_external` + 权限）即可修复外链**；不需要改 Rust 代码，不阻塞 M12。待有 Windows 构建环境时重打安装包验证。

## 二、架构与实现要点

1. **软删 + 排序**：删除置 `deletedAt`（与便签习惯一致），查询默认过滤 `deletedAt: null`，排序 `date asc, allDay desc, startTime asc, createdAt asc`。`date` 用 `YYYY-MM-DD` 字符串避免时区错位。
2. **单页状态机**：`CalendarPageClient` 维护 month/week/day 三视图 + `rangeFor` 计算拉取范围，`useCalendarEvents` 用稳定 `useCallback` + 乐观更新（update 失败回滚）。
3. **便签关联**：`EventNotePicker` 独立实例搜索（`/api/notes?q=`），选中便签以芯片显示，可取消；编辑已有事件时通过 `noteTitle` 回填标题（不依赖搜索）。
4. **今日条点击跳转**：事件带便签 → 打开便签入返回栈；无便签 → 打开编辑弹窗；「查看日历」→ `/calendar` 定位今天。

## 三、改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `prisma/schema.prisma` | 新增 `CalendarEvent` 模型 + `User.calendarEvents` / `Note.calendarEvents` 关系 |
| 修改 | `src/types/index.ts` | `CalendarEvent` / `CalendarEventInput` / `CalendarEventStatus` 类型 |
| 新增 | `src/app/(protected)/calendar/page.tsx` | 服务端页：`requireAdmin()` 失败 `redirect("/?notice=calendar_forbidden")` |
| 新增 | `src/components/calendar/CalendarPageClient.tsx` | 三视图状态机 + 顶部工具栏 + 事件弹窗调度 |
| 新增 | `src/components/calendar/MonthView.tsx` | 月网格（≤3 事件卡片/窄窗口圆点降级/+N/今天高亮） |
| 新增 | `src/components/calendar/WeekView.tsx` | 周视图（7 天，全天/带时间排序） |
| 新增 | `src/components/calendar/TodayList.tsx` | 今日列表（勾选完成/新建/点击） |
| 新增 | `src/components/calendar/EventChip.tsx` | 事件小卡片 |
| 新增 | `src/components/calendar/EventEditorDialog.tsx` | 新建/编辑弹窗（删除仅编辑态，confirm 提示不删便签） |
| 新增 | `src/components/calendar/EventNotePicker.tsx` | 便签搜索选择器 |
| 新增 | `src/components/calendar/TodayStrip.tsx` | 首页今日条 |
| 新增 | `src/hooks/useCalendarEvents.ts` | 拉取/创建/更新/删除 + 乐观更新 |
| 新增 | `src/hooks/useCurrentUser.ts` | 当前用户（admin 判断用） |
| 新增 | `src/lib/calendar-date.ts` | 日期工具（toDateStr/fromDateStr/todayStr/startOfWeek/monthGrid/…） |
| 新增 | `src/lib/calendar.ts` | 事件排序/格式化辅助 |
| 新增 | `src/app/api/calendar-events/route.ts` | GET 范围 / POST |
| 新增 | `src/app/api/calendar-events/[id]/route.ts` | PATCH / DELETE |
| 修改 | `src/components/Sidebar.tsx` | `isAdmin` prop → 管理员「日历」入口 |
| 修改 | `src/app/(protected)/page.tsx` | `useCurrentUser` + admin 判断渲染 TodayStrip + `notice=calendar_forbidden` toast |
| 修改 | `src/hooks/useSyncStatus.ts` | **顺手修复既有 SSR 水合 bug**（见四、附带修复） |

## 四、附带修复（既有问题，非 M12 引入）

1. **`useSyncStatus.ts` SSR 水合错位**（历史遗留）：Next.js 在 SSR 渲染 client 组件时会 polyfill `navigator`，使 `navigator.onLine` 在服务端返回 false，与浏览器端不一致 → 首页 `OfflineBar` 水合错位（SSR 渲染「当前离线」、客户端渲染 null → hydration error）。改为 `typeof window !== "undefined"` 判断后，SSR 默认按「在线」渲染，修复首页水合。
2. **`EventNotePicker` 两个 bug**（本轮验收发现并修复）：
   - 搜索永远空结果：接口返回 `{ notes: [...] }`（无 `ok` 字段），原判断 `!data.ok` 恒真 → 下拉永远「没有匹配的便签」。改为 `!Array.isArray(data.notes)`。
   - 选中便签显示「未命名便签」：`handlePick` 清空 results 后 `[value]` effect 重跑，用空 results 把选中项覆盖成 `{ title: "" }` 占位。改为函数式 `setSelected`（`prev.id === value` 保留），并用新 prop `noteTitle` 回填编辑已有事件的标题。

## 五、验证结果

### 本地编译
- `npx tsc --noEmit`：通过（先 build 后 tsc，避免 `.next/types` 缓存干扰）。
- `npm run build -- --webpack`：通过（生产构建必须 webpack）。
- `npx prisma generate` / `npx prisma db push`：本地通过，`CalendarEvent` 表创建成功。

### 浏览器实测（dev，localhost:3000）
**管理员（admin/admin123）：**
| 验收项 | 结果 |
|---|---|
| 月视图：3 条种子事件（全天回顾/今日会议-已编辑/周五评审）正确渲染 | ✅ |
| 周视图排除下周二事件、今日列表过滤当天 | ✅ |
| 新建事件（含时间 09:30-10:30、全天开关）→ 保存后出现在月视图与今日条 | ✅ |
| 编辑事件（标题改、状态 done 切换）乐观更新 | ✅ |
| 删除事件（confirm 后软删，API 不再返回） | ✅ |
| 便签关联：搜索「Debug」→ 选中 → 芯片显示「Debug test」（修复前「未命名便签」）；保存后 API `noteId` 与 `note.title` 正确 | ✅ |
| 编辑已有事件：已关联便签回填显示「Debug test」（noteTitle 路径） | ✅ |
| 首页今日条：显示今天事件「全天回顾」，可跳日历 | ✅ |
| 侧边栏「日历」入口进入日历页 | ✅ |
| 320px 窄窗口：月视图日期+圆点降级，`scrollWidth === clientWidth` 无横向溢出 | ✅ |

**普通用户（m12_403_test@example.com）：**
| 验收项 | 结果 |
|---|---|
| 侧边栏无「日历」入口、首页无今日条 | ✅ |
| 直接访问 `/calendar` → 重定向回 `/` + toast「无权限访问日历」（2s 后自动清理 query） | ✅ |
| `GET /api/calendar-events` 返回 403 FORBIDDEN | ✅ |

## 六、生产部署

1. 备份：`data/prod.db → backups/prod_M12_20260809_191503.db`（282624 B）。
2. rsync `src/ --delete` + 同步**两个** prisma schema 文件（`prisma/schema.prisma` 与根目录 `schema.prisma`，规避双 schema 遮蔽陷阱）。
3. 服务器 `npx prisma generate` + `npx prisma db push`：`CalendarEvent` 表在 `data/prod.db` 确认创建（13 列含 userId/noteId/deletedAt 等）。
4. `rm -rf .next && npm run build -- --webpack`。
5. 重启 `sticky-notes.service`。
6. 健康检查见下。

### 健康检查

| 检查项 | 结果 |
|---|---|
| `GET /login` | 200 ✅ |
| 未登录 `GET /` | 307（跳登录页）✅ |
| 未登录 `GET /api/auth/me` | 401 ✅ |
| admin 登录（`admin/ding2860`） | 200 ✅ |
| 创建事件（含 noteId 关联「收藏」） | `{ok:true, event:{...}}` ✅ |
| GET 范围接口回读 | 事件与 `note.title` 正确返回 ✅ |
| DELETE 软删 | `{ok:true}`，再 GET 范围 count=0 ✅ |

## 七、风险与后续建议

- **AI 排期导入（M13）**：模型已预留 `source`、`description`、`noteId`、批量创建扩展空间；M13 在 M12 数据结构上做 AI 解析 + 确认页 + 批量写入。
- **Windows 壳外链**：桌面壳 P1，待重打 Windows 安装包（含 `a16999b`）后验证。
- **冲突检测/拖拽**：M12 明确不做，`startTime/endTime` 仅展示与排序。
- 便签删除后事件不删除、关联显示「便签已失效」：按 SPEC 八，M12 已具备基础（`note.isTrashed` 判定），复杂交互留后续。

## 八、任务完成情况

SPEC 十二的 7 项拆分全部完成；「Windows 壳外链排查」在时间盒内收口并记录为桌面壳 P1。

## 九、QA 视觉返修（2026-08-09，同日晚间）

按 `docs/QA_M12_工作日历入口与视觉返修_20260809.md` 返修。QA 判定 M12 底子正确但**入口与视觉体验不通过**：日历入口是普通导航链接、首页顶部多一条今日条，挤压了主界面，不符合轻量便签产品气质。本轮只做入口收敛与视觉统一，不新增功能。

### 9.1 返修结论

| 项 | 返修前 | 返修后 |
|---|---|---|
| 日历入口 | 搜索框下方一行普通「日历」链接 | 左侧侧边栏顶部**日历卡片**（仅管理员） |
| 首页今日条 | `TodayStrip`（顶部全局条） | **已删除**，今日信息沉入日历卡片 |
| 左侧链接预览 | 显示 `[[note:id|title]]` 代码 | 只显示 `title`（转义标题还原 `A]B`） |

### 9.2 改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 新增 | `src/components/calendar/CalendarCard.tsx` | 侧边栏日历卡片：工作日历 + 今天日期 + 今日未完成数 + 最多 2 条事件（无事件→「今天暂无事件」）+ 底部「+ 添加今日事件」；卡片主体点击进 `/calendar`，`+` 进 `/calendar?date=YYYY-MM-DD&new=1`；独立 `useCalendarEvents` 实例拉今天，聚焦刷新 |
| 修改 | `src/components/Sidebar.tsx` | 删除普通「日历」链接，`isAdmin` 时在搜索行下方、便签列表上方渲染 `<CalendarCard />` |
| 修改 | `src/app/(protected)/page.tsx` | 删除 `TodayStrip` 挂载（import + 使用块 + `onOpenCalendar`），顺带移除不再用的 `useRouter` |
| 删除 | `src/components/calendar/TodayStrip.tsx` | 首页今日条组件（无任何引用后删除） |
| 修改 | `src/lib/link-parser.ts` | 新增 `displayPlainText`：`[[note:id|title]]` → `title`，转义标题 `A\]B` → `A]B`，外部 URL 保持原文本，残缺/非法内链保持原样 |
| 修改 | `src/components/NoteList.tsx` | `getAutoTitle` / `getSummaryLines` 输出的标题与摘要过 `displayPlainText`，搜索高亮在替换后的文本上生效 |

### 9.3 视觉验收（浏览器实测，admin，dev + 生产同代码）

| 验收项 | 结果 |
|---|---|
| 日历入口是卡片（`card-surface` + `rounded-card` 10px），与便签卡片统一 | ✅ |
| 卡片位于搜索框与便签列表之间，未挤压便签列表/编辑器 | ✅ |
| 卡片显示「工作日历 今天 / 8 月 9 日 · 周日 · 今日未完成 N 项 / 最多 2 条事件」 | ✅ |
| 无事件时显示「今天暂无事件」（生产当日无事件，卡片正确显示空态文案） | ✅ |
| 点击卡片主体 → `/calendar`（客户端导航成功） | ✅ |
| 点击「+ 添加今日事件」→ `/calendar?date=2026-08-09&new=1` 并打开新建事件弹窗 | ✅ |
| 首页顶部无 `TodayStrip`、无「查看日历」残留 | ✅ |
| 320px 窄窗口：卡片 295px 宽、事件标题 truncate 不溢出、无页面级横向滚动（`scrollWidth===clientWidth=320`） | ✅ |
| 左侧链接预览：`[[note:…|客户反馈]]` 显示「客户反馈」、转义标题 `A\]B` 显示「A]B」、外链保留 URL 文本、无 `[[note:` 裸代码 | ✅ |
| 搜索高亮：搜「客户反馈」时 `<mark>` 高亮落在替换后的文本上 | ✅ |
| 普通用户：侧边栏无日历卡片、首页无今日条、直接访问 `/calendar` 重定向回 `/` + toast「无权限访问日历」、`GET /api/calendar-events` 403 | ✅ |

### 9.4 构建与部署

- 本地 `npx tsc --noEmit`：通过；`npm run build -- --webpack`：通过。
- 生产部署：备份 `prod_M12_rework_20260809_194607.db` → rsync `src/ --delete` → `rm -rf .next && npm run build -- --webpack` → 重启 `sticky-notes.service`（active）。
- 健康检查：`/login` 200、未登录 `/` 307→`/login`、`/api/auth/me` 未登录 401、admin 登录（`ding2860`）200、admin `GET /api/calendar-events` `{ok:true}`、含内链便签 API 创建/删除 round-trip `{ok:true}`。
- 无 schema 改动，无 DB 迁移。

### 9.5 未做项

- AI 排期导入、拖拽事件、提醒通知、复杂项目管理均不做（与 QA §3.2 一致）。
- 不恢复极小模式/悬浮窗。
- 不把日历做进编辑器、不新增首页/编辑器顶部日历入口。

### 9.6 已知问题（预存在，非本轮引入）

- **侧边栏宽度过渡偶发卡在 350px**：双栏 350px ↔ 单栏 100% 之间的 `transition-[width,opacity]` 在窄窗口（<720px）加载/缩窗时可能停在 350px 不收敛（`getComputedStyle` 一直报 350px），导致 320px 下侧边栏右侧约 30px 被 `overflow-hidden` 裁切。**不影响卡片本身**：关闭过渡后卡片 295px、事件标题 truncate、无页面级横向滚动。该问题位于 `page.tsx` 侧边栏包裹 div（本次未改动），属于 M12 之前的既有行为，记录为后续排期项，不在本轮返修范围。
