# M12 交付报告：工作日历基础层 + Windows 壳外链排查

日期：2026-08-09

## 一、交付内容

按 `docs/SPEC_M12_工作日历基础层_20260809.md` 实现工作日历基础层，管理员灰度上线：

- **结构化数据模型**：新增 `CalendarEvent`（title/date/startTime/endTime/allDay/status/source/noteId 关联/软删 `deletedAt`），`source` 固定 `manual` 为 M13 AI 导入预留。
- **管理员专用 CRUD API**：`GET /api/calendar-events?from&to`、`POST /api/calendar-events`、`PATCH /api/calendar-events/[id]`、`DELETE /api/calendar-events/[id]`。全部 `requireAdmin()`：未登录 401、普通用户 403、只能操作自己的事件、noteId 校验（属于当前用户且未删除）。
- **日历页** `/calendar`（管理员侧边栏入口）：月视图（7 列网格、≤3 事件卡片、+N、今天高亮）、周视图、今日列表（勾选完成/新建/点击事件）。320px 窄窗口降级为日期+圆点，无重叠、无横向溢出。
- **事件编辑弹窗**：标题/日期/全天开关/起止时间/状态/关联便签/备注；便签选择复用便签搜索（标题+正文匹配）。
- **首页今日条**：仅管理员展示——今日日期、未完成数量、前 3 条事件、「查看日历」「+今日」入口；点击事件有关联便签打开便签，否则打开编辑弹窗。
- **普通用户灰度**：无侧边栏入口、无今日条、直接访问 `/calendar` 重定向回首页并提示「无权限访问日历」、日历 API 返回 403。

### Windows 壳外链排查（小排查，时间盒内收口）

按 SPEC 二、2.2 做了小范围排查，结论：

- `src-tauri/src/lib.rs` 已存在 `open_external` 与 `open_external_system` 命令；`permissions/default.toml` 与 `capabilities/default.json` 已含 `allow-open-external`。
- **根因指向旧包**：Windows 桌面壳当前安装的安装包早于 `open_external` 命令合入的提交（`a16999b`），旧包没有该命令 → 前端 `invoke("open_external")` 抛错、静默失败。
- 处置：**记录为桌面壳 P1：重打 Windows 包（含 `a16999b` 后的 `open_external` + 权限）即可修复外链**；不需要改 Rust 代码，不阻塞 M12。待有 Windows 构建环境时重打安装包验证。

## 二、架构与实现要点

1. **软删 + 排序**：删除置 `deletedAt`（与便签习惯一致），查询默认过滤 `deletedAt: null`，排序 `date asc, allDay desc, startTime asc, createdAt asc`。`date` 用 `YYYY-MM-DD` 字符串避免时区错位。
2. **单页状态机**：`CalendarPageClient` 维护 month/week/day 三视图 + `rangeFor` 计算拉取范围，`useCalendarEvents` 用稳定 `useCallback` + 乐观更新（update 失败回滚）。
3. **便签关联**：`EventNotePicker` 独立实例搜索（`/api/notes?q=`），选中便签以芯片显示，可取消；编辑已有事件时通过 `noteTitle` 回填标题（不依赖搜索）。
4. **今日条点击跳转**：事件带便签 → 打开便签入返回栈；无便签 → 打开编辑弹窗；「查看日历」→ `/calendar` 定位今天。

## 三、改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `prisma/schema.prisma` | 新增 `CalendarEvent` 模型 + `User.calendarEvents` / `Note.calendarEvents` 关系 |
| 修改 | `src/types/index.ts` | `CalendarEvent` / `CalendarEventInput` / `CalendarEventStatus` 类型 |
| 新增 | `src/app/(protected)/calendar/page.tsx` | 服务端页：`requireAdmin()` 失败 `redirect("/?notice=calendar_forbidden")` |
| 新增 | `src/components/calendar/CalendarPageClient.tsx` | 三视图状态机 + 顶部工具栏 + 事件弹窗调度 |
| 新增 | `src/components/calendar/MonthView.tsx` | 月网格（≤3 事件卡片/窄窗口圆点降级/+N/今天高亮） |
| 新增 | `src/components/calendar/WeekView.tsx` | 周视图（7 天，全天/带时间排序） |
| 新增 | `src/components/calendar/TodayList.tsx` | 今日列表（勾选完成/新建/点击） |
| 新增 | `src/components/calendar/EventChip.tsx` | 事件小卡片 |
| 新增 | `src/components/calendar/EventEditorDialog.tsx` | 新建/编辑弹窗（删除仅编辑态，confirm 提示不删便签） |
| 新增 | `src/components/calendar/EventNotePicker.tsx` | 便签搜索选择器 |
| 新增 | `src/components/calendar/TodayStrip.tsx` | 首页今日条 |
| 新增 | `src/hooks/useCalendarEvents.ts` | 拉取/创建/更新/删除 + 乐观更新 |
| 新增 | `src/hooks/useCurrentUser.ts` | 当前用户（admin 判断用） |
| 新增 | `src/lib/calendar-date.ts` | 日期工具（toDateStr/fromDateStr/todayStr/startOfWeek/monthGrid/…） |
| 新增 | `src/lib/calendar.ts` | 事件排序/格式化辅助 |
| 新增 | `src/app/api/calendar-events/route.ts` | GET 范围 / POST |
| 新增 | `src/app/api/calendar-events/[id]/route.ts` | PATCH / DELETE |
| 修改 | `src/components/Sidebar.tsx` | `isAdmin` prop → 管理员「日历」入口 |
| 修改 | `src/app/(protected)/page.tsx` | `useCurrentUser` + admin 判断渲染 TodayStrip + `notice=calendar_forbidden` toast |
| 修改 | `src/hooks/useSyncStatus.ts` | **顺手修复既有 SSR 水合 bug**（见四、附带修复） |

## 四、附带修复（既有问题，非 M12 引入）

1. **`useSyncStatus.ts` SSR 水合错位**（历史遗留）：Next.js 在 SSR 渲染 client 组件时会 polyfill `navigator`，使 `navigator.onLine` 在服务端返回 false，与浏览器端不一致 → 首页 `OfflineBar` 水合错位（SSR 渲染「当前离线」、客户端渲染 null → hydration error）。改为 `typeof window !== "undefined"` 判断后，SSR 默认按「在线」渲染，修复首页水合。
2. **`EventNotePicker` 两个 bug**（本轮验收发现并修复）：
   - 搜索永远空结果：接口返回 `{ notes: [...] }`（无 `ok` 字段），原判断 `!data.ok` 恒真 → 下拉永远「没有匹配的便签」。改为 `!Array.isArray(data.notes)`。
   - 选中便签显示「未命名便签」：`handlePick` 清空 results 后 `[value]` effect 重跑，用空 results 把选中项覆盖成 `{ title: "" }` 占位。改为函数式 `setSelected`（`prev.id === value` 保留），并用新 prop `noteTitle` 回填编辑已有事件的标题。

## 五、验证结果

### 本地编译
- `npx tsc --noEmit`：通过（先 build 后 tsc，避免 `.next/types` 缓存干扰）。
- `npm run build -- --webpack`：通过（生产构建必须 webpack）。
- `npx prisma generate` / `npx prisma db push`：本地通过，`CalendarEvent` 表创建成功。

### 浏览器实测（dev，localhost:3000）
**管理员（admin/admin123）：**
| 验收项 | 结果 |
|---|---|
| 月视图：3 条种子事件（全天回顾/今日会议-已编辑/周五评审）正确渲染 | ✅ |
| 周视图排除下周二事件、今日列表过滤当天 | ✅ |
| 新建事件（含时间 09:30-10:30、全天开关）→ 保存后出现在月视图与今日条 | ✅ |
| 编辑事件（标题改、状态 done 切换）乐观更新 | ✅ |
| 删除事件（confirm 后软删，API 不再返回） | ✅ |
| 便签关联：搜索「Debug」→ 选中 → 芯片显示「Debug test」（修复前「未命名便签」）；保存后 API `noteId` 与 `note.title` 正确 | ✅ |
| 编辑已有事件：已关联便签回填显示「Debug test」（noteTitle 路径） | ✅ |
| 首页今日条：显示今天事件「全天回顾」，可跳日历 | ✅ |
| 侧边栏「日历」入口进入日历页 | ✅ |
| 320px 窄窗口：月视图日期+圆点降级，`scrollWidth === clientWidth` 无横向溢出 | ✅ |

**普通用户（m12_403_test@example.com）：**
| 验收项 | 结果 |
|---|---|
| 侧边栏无「日历」入口、首页无今日条 | ✅ |
| 直接访问 `/calendar` → 重定向回 `/` + toast「无权限访问日历」（2s 后自动清理 query） | ✅ |
| `GET /api/calendar-events` 返回 403 FORBIDDEN | ✅ |

## 六、生产部署

1. 备份：`data/prod.db → backups/prod_M12_20260809_191503.db`（282624 B）。
2. rsync `src/ --delete` + 同步**两个** prisma schema 文件（`prisma/schema.prisma` 与根目录 `schema.prisma`，规避双 schema 遮蔽陷阱）。
3. 服务器 `npx prisma generate` + `npx prisma db push`：`CalendarEvent` 表在 `data/prod.db` 确认创建（13 列含 userId/noteId/deletedAt 等）。
4. `rm -rf .next && npm run build -- --webpack`。
5. 重启 `sticky-notes.service`。
6. 健康检查见下。

### 健康检查

| 检查项 | 结果 |
|---|---|
| `GET /login` | 200 ✅ |
| 未登录 `GET /` | 307（跳登录页）✅ |
| 未登录 `GET /api/auth/me` | 401 ✅ |
| admin 登录（`admin/ding2860`） | 200 ✅ |
| 创建事件（含 noteId 关联「收藏」） | `{ok:true, event:{...}}` ✅ |
| GET 范围接口回读 | 事件与 `note.title` 正确返回 ✅ |
| DELETE 软删 | `{ok:true}`，再 GET 范围 count=0 ✅ |

## 七、风险与后续建议

- **AI 排期导入（M13）**：模型已预留 `source`、`description`、`noteId`、批量创建扩展空间；M13 在 M12 数据结构上做 AI 解析 + 确认页 + 批量写入。
- **Windows 壳外链**：桌面壳 P1，待重打 Windows 安装包（含 `a16999b`）后验证。
- **冲突检测/拖拽**：M12 明确不做，`startTime/endTime` 仅展示与排序。
- 便签删除后事件不删除、关联显示「便签已失效」：按 SPEC 八，M12 已具备基础（`note.isTrashed` 判定），复杂交互留后续。

## 八、任务完成情况

SPEC 十二的 7 项拆分全部完成；「Windows 壳外链排查」在时间盒内收口并记录为桌面壳 P1。

## 九、QA 视觉返修（2026-08-09，同日晚间）

按 `docs/QA_M12_工作日历入口与视觉返修_20260809.md` 返修。QA 判定 M12 底子正确但**入口与视觉体验不通过**：日历入口是普通导航链接、首页顶部多一条今日条，挤压了主界面，不符合轻量便签产品气质。本轮只做入口收敛与视觉统一，不新增功能。

### 9.1 返修结论

| 项 | 返修前 | 返修后 |
|---|---|---|
| 日历入口 | 搜索框下方一行普通「日历」链接 | 左侧侧边栏顶部**日历卡片**（仅管理员） |
| 首页今日条 | `TodayStrip`（顶部全局条） | **已删除**，今日信息沉入日历卡片 |
| 左侧链接预览 | 显示 `[[note:id|title]]` 代码 | 只显示 `title`（转义标题还原 `A]B`） |

### 9.2 改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 新增 | `src/components/calendar/CalendarCard.tsx` | 侧边栏日历卡片：工作日历 + 今天日期 + 今日未完成数 + 最多 2 条事件（无事件→「今天暂无事件」）+ 底部「+ 添加今日事件」；卡片主体点击进 `/calendar`，`+` 进 `/calendar?date=YYYY-MM-DD&new=1`；独立 `useCalendarEvents` 实例拉今天，聚焦刷新 |
| 修改 | `src/components/Sidebar.tsx` | 删除普通「日历」链接，`isAdmin` 时在搜索行下方、便签列表上方渲染 `<CalendarCard />` |
| 修改 | `src/app/(protected)/page.tsx` | 删除 `TodayStrip` 挂载（import + 使用块 + `onOpenCalendar`），顺带移除不再用的 `useRouter` |
| 删除 | `src/components/calendar/TodayStrip.tsx` | 首页今日条组件（无任何引用后删除） |
| 修改 | `src/lib/link-parser.ts` | 新增 `displayPlainText`：`[[note:id|title]]` → `title`，转义标题 `A\]B` → `A]B`，外部 URL 保持原文本，残缺/非法内链保持原样 |
| 修改 | `src/components/NoteList.tsx` | `getAutoTitle` / `getSummaryLines` 输出的标题与摘要过 `displayPlainText`，搜索高亮在替换后的文本上生效 |

### 9.3 视觉验收（浏览器实测，admin，dev + 生产同代码）

| 验收项 | 结果 |
|---|---|
| 日历入口是卡片（`card-surface` + `rounded-card` 10px），与便签卡片统一 | ✅ |
| 卡片位于搜索框与便签列表之间，未挤压便签列表/编辑器 | ✅ |
| 卡片显示「工作日历 今天 / 8 月 9 日 · 周日 · 今日未完成 N 项 / 最多 2 条事件」 | ✅ |
| 无事件时显示「今天暂无事件」（生产当日无事件，卡片正确显示空态文案） | ✅ |
| 点击卡片主体 → `/calendar`（客户端导航成功） | ✅ |
| 点击「+ 添加今日事件」→ `/calendar?date=2026-08-09&new=1` 并打开新建事件弹窗 | ✅ |
| 首页顶部无 `TodayStrip`、无「查看日历」残留 | ✅ |
| 320px 窄窗口：卡片 295px 宽、事件标题 truncate 不溢出、无页面级横向滚动（`scrollWidth===clientWidth=320`） | ✅ |
| 左侧链接预览：`[[note:…|客户反馈]]` 显示「客户反馈」、转义标题 `A\]B` 显示「A]B」、外链保留 URL 文本、无 `[[note:` 裸代码 | ✅ |
| 搜索高亮：搜「客户反馈」时 `<mark>` 高亮落在替换后的文本上 | ✅ |
| 普通用户：侧边栏无日历卡片、首页无今日条、直接访问 `/calendar` 重定向回 `/` + toast「无权限访问日历」、`GET /api/calendar-events` 403 | ✅ |

### 9.4 构建与部署

- 本地 `npx tsc --noEmit`：通过；`npm run build -- --webpack`：通过。
- 生产部署：备份 `prod_M12_rework_20260809_194607.db` → rsync `src/ --delete` → `rm -rf .next && npm run build -- --webpack` → 重启 `sticky-notes.service`（active）。
- 健康检查：`/login` 200、未登录 `/` 307→`/login`、`/api/auth/me` 未登录 401、admin 登录（`ding2860`）200、admin `GET /api/calendar-events` `{ok:true}`、含内链便签 API 创建/删除 round-trip `{ok:true}`。
- 无 schema 改动，无 DB 迁移。

### 9.5 未做项

- AI 排期导入、拖拽事件、提醒通知、复杂项目管理均不做（与 QA §3.2 一致）。
- 不恢复极小模式/悬浮窗。
- 不把日历做进编辑器、不新增首页/编辑器顶部日历入口。

### 9.6 已知问题（预存在，非本轮引入）

- **侧边栏宽度过渡偶发卡在 350px**：双栏 350px ↔ 单栏 100% 之间的 `transition-[width,opacity]` 在窄窗口（<720px）加载/缩窗时可能停在 350px 不收敛（`getComputedStyle` 一直报 350px），导致 320px 下侧边栏右侧约 30px 被 `overflow-hidden` 裁切。**不影响卡片本身**：关闭过渡后卡片 295px、事件标题 truncate、无页面级横向滚动。该问题位于 `page.tsx` 侧边栏包裹 div（本次未改动），属于 M12 之前的既有行为，记录为后续排期项，不在本轮返修范围。

---

## 十、QA 视觉返修 R2（2026-08-09，晚间第二轮）

按用户要求做第二轮体验结构返修。R1 的左侧链接预览修复通过，但**工作日历入口位置与交互仍不符合要求**：R1 把入口放侧边栏最顶层（搜索框下方）独立一张卡、点击跳 `/calendar` 独立页，脱离了便签工作台。本轮只调整入口与右侧展示结构，不扩 AI 导入、不扩日历功能。

### 10.1 返修结论

| 项 | 返修前（R1） | 返修后（R2） |
|---|---|---|
| 入口位置 | 侧边栏最顶层、搜索框下方一张独立卡 | 并入左侧列表结构：原来「置顶」分组位置改为「日历」分组，卡片作为分组内容，置顶/日期分组顺位下移 |
| 点击行为 | 跳转 `/calendar` 独立页面 | 不跳转，URL 保持 `/`，在右侧主内容区嵌入日历空间（复用 CalendarPageClient 月/周/今日） |
| 单栏（<720px） | 独立页整屏 | 点入口 → 侧栏收起、日历占满右面板，头部「返回」关闭日历回列表 |
| 选中态 | 无 | 日历打开时入口卡片呈选中态（`card-selected card-selected-gray`） |
| 管理员空列表 | 入口可见（R1 卡片在列表外） | 空列表下入口仍在列表内可见，下方行内空提示 |

### 10.2 架构调整

1. **`CalendarPageClient` 改为可嵌入组件**：去掉 `useSearchParams`（需 Suspense 边界、且嵌入态拿不到 URL 深链），改为 props 驱动：
   - `onBack`：单栏嵌入态点击返回关闭面板；不传则嵌入双栏不显示返回区。
   - `embedded`：true → 根节点 `flex-1 h-full` 填充右面板；false → `h-screen` 独立页。
   - `openNewEvent: { date, n }`：嵌入态由父级「+ 添加今日事件」触发，`n` 递增可重复弹出。
   - `initialAction: { date, newEvent, eventId }`：独立页从 URL 深链读到的初始动作，挂载时生效一次。
2. **`/calendar` 独立页保留但不再作入口**：`searchParams` 改为 `await`（Next 16 为 Promise）转 `initialAction`，删除 `<Suspense>`。管理员直接访问仍可用（月视图 + 深链 `?date=…&new=1` / `?event=…`）；普通用户仍 `requireAdmin()` 失败 → `redirect("/?notice=calendar_forbidden")` + toast + API 403。
3. **入口移入 `NoteList`**：列表滚动区顶部渲染「日历」分组（`SectionHeader` + `CalendarCard`），随后才是置顶/日期分组；空态重构——管理员空列表在滚动区内「日历分组 + 行内空提示」，普通用户空列表仍居中空态。

### 10.3 改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `src/components/calendar/CalendarPageClient.tsx` | 新增 props `onBack/embedded/openNewEvent/initialAction`；删除 `useSearchParams` 与深链 effect，改 initialAction 挂载 effect + openNewEvent 变化 effect；根节点 class 按 `embedded` 切换；头部返回区：`onBack` 按钮 / 独立页「返回便签」Link / 嵌入双栏不显示 |
| 修改 | `src/app/(protected)/calendar/page.tsx` | `searchParams` `await` 后取 `date/new/event` 传 `initialAction`；删除 `<Suspense>` |
| 修改 | `src/components/calendar/CalendarCard.tsx` | 删除两个 `<Link href="/calendar…">`，改 `onOpen/onOpenNew` 按钮回调 + `active` 选中态；外层 `mx-3` 去掉改 `w-full`（现在在 NoteList 的 `px-3` 容器内） |
| 修改 | `src/components/NoteList.tsx` | 新增 props `isAdmin/onOpenCalendar/onOpenCalendarNew/calendarActive`；滚动区顶部「日历」分组；空态重构（管理员空列表入口仍可见）；`pinned/regularGroups` 计算上移到空态判断前 |
| 修改 | `src/components/Sidebar.tsx` | 删除 `CalendarCard` import 与渲染块，改为透传 `onOpenCalendar/onOpenCalendarNew/calendarActive` 给 NoteList |
| 修改 | `src/app/(protected)/page.tsx` | 新增 `calendarOpen/calendarNewEvent` 状态 + `openCalendar({newEvent?})/closeCalendar`；`handleSelect/handleCreate` 关闭日历；单栏侧栏隐藏条件 `!showEditor` → `!showEditor && !calendarOpen`；右面板 `calendarOpen ? <CalendarPageClient embedded …/> : <NoteEditor/>`；**顺手修复既有侧边栏宽度过渡卡死 bug（见 10.5）** |

### 10.4 视觉验收（浏览器实测，dev，admin + 普通用户）

**管理员：**
| 验收项 | 结果 |
|---|---|
| 侧边栏顶部为「日历」分组（SectionHeader + 入口卡片），置顶/日期分组顺位下移 | ✅ |
| 点击入口卡片 → **URL 不变**（仍 `/`），右面板显示嵌入日历月视图；卡片呈选中态 | ✅ |
| 点某条便签 → 回到编辑器、日历关闭、卡片取消选中 | ✅ |
| 点卡片「+ 添加今日事件」→ 右面板打开且新建事件弹窗默认今天（2026-08-09），可重复触发 | ✅ |
| 嵌入态月/周/今日视图正常（周视图今天显示 2 条事件） | ✅ |
| 单栏（390px）点入口 → 侧栏收起至 0、日历占满 390px；头部「返回」关闭日历回列表 | ✅ |
| 单栏点「+ 添加今日事件」→ 日历占满 + 弹窗默认今天 | ✅ |
| 双栏（1440px）点入口 → 侧栏保持 350px、日历占右面板，无返回按钮（符合嵌入交互） | ✅ |
| 缩窗 1440→390 与 390→1440（日历开着）：侧栏即时收敛，无卡 350px、无卡 0px（修复生效） | ✅ |
| 320px：入口卡片 295px 无溢出，嵌入日历页 `scrollWidth===clientWidth=320` | ✅ |
| 独立页 `/calendar` 对管理员仍可用（返回便签 Link + 月视图）；`/calendar?date=…&new=1` 打开新建弹窗 | ✅ |

**普通用户（m12_403_test）：**
| 验收项 | 结果 |
|---|---|
| 列表无「日历」分组、无入口卡片 | ✅ |
| 直接访问 `/calendar` → 重定向回 `/` + toast「无权限访问日历」 | ✅ |
| `GET /api/calendar-events` 返回 403 | ✅ |

### 10.5 顺手修复：侧边栏宽度过渡卡死（既有 bug，R2 触碰同段代码）

R2 要求「单栏点入口 → 侧栏收起、日历占满」，实测发现 R1 §9.6 记录的既有 bug 会直接破坏该体验：侧边栏包裹 div 的 `transition-[width,opacity]` 在「双栏 350px ↔ 单栏 100%」以及「transition-property 与宽度同帧变化」时被浏览器冻结在起始值（`getAnimations()` 显示 width/opacity 动画 currentTime=0、永久 running），侧栏既不收起也不展开，日历被挤到 151px。

**修复**：移除该 div 的 width/opacity 过渡（侧栏收起改为即时）。150ms 动画收益远低于反复卡死的代价，且即时收起对移动端更干脆。修复后验证：单栏 390px 侧栏精确 390→0 收敛、1440px 350→0 收敛、双向缩窗无卡死、`getAnimations()` 为空。普通便签编辑/返回流程同步回归通过。

### 10.6 构建与部署

- 本地 `npx tsc --noEmit`：通过；`npm run build -- --webpack`：通过（含侧栏过渡修复后的二次构建）。
- 生产部署：备份 `prod_M12_R2_20260809_201328.db`（303104 B）→ rsync `src/ --delete` → 服务器 `rm -rf .next && npm run build -- --webpack` → 重启 `sticky-notes.service`（active）。
- 健康检查：`/login` 200、未登录 `/` 307→`/login`、`/api/auth/me` 未登录 401、admin 登录（`ding2860`）200、`GET /api/calendar-events` `{ok:true}`、日历事件 create/delete round-trip `{ok:true}` 且重读为空、便签 create/delete round-trip 200。
- 无 schema 改动，无 DB 迁移。

### 10.7 未做项

- 不扩 AI 排期导入、不扩日历功能（冲突检测/拖拽/提醒均不做，与 QA §3.2 一致）。
- `/calendar` 独立页保留但不再是入口，仅管理员深链/直接访问可用。
- 不恢复极小模式/悬浮窗；不把日历做进编辑器。

---

## 十一、QA 视觉一比一返修（2026-08-09，晚间第三轮）

按 `docs/QA_M12_工作日历视觉一比一返修_20260809.md` 做视觉一比一返修。QA 判定 M12 功能骨架已有但视觉完成度不达标（整体像工程表格小组件）。本轮以原型 `/Users/dian/.codex/visualizations/2026/08/06/019fd7e6-8a92-7780-80ce-ef4cfb9e710b/work-calendar-interactive-prototype.html` 为基准做信息层级与视觉一比一：**不改后端主模型、不新增 AI 导入**，聚焦前端视觉与交互完成度。

### 11.1 返修结论

| 项 | 返修前 | 返修后 |
|---|---|---|
| 整体布局 | 三视图共用一个 main，右侧详情/编辑以弹窗承载 | **三栏工作空间**：左侧便签栏 + 日历主区 + 常驻 300px 右侧详情/编辑面板（窄屏变抽屉） |
| 顶部区 | 工作日历 + 周/月/今日 + 上下页 + 今天 + 新增排期 + 新增项目 + 快速录入等 6-8 个控件挤一行 | **三行分区**：① 标题 + 次级说明 + 上下页/今天/月份选择 + 今日/月/周 segmented control；② 项目筛选 chip + 便签链接开关 + 新增排期/新增项目/快速录入 + 显示归档；③ 指标行（当前显示/连续阶段/单日节点/便签链接） |
| 项目筛选 | 便签链接开关只在今日视图 | 顶部 filter chip（项目色点）+ **便签链接全局开关**，月/周/今日/指标/左侧预览全同步 |
| 月视图 | 每周日期行上方另贴一排小条 | **月历格内轻量甘特层**：104px 定高格、3 分轨、跨周/跨月拆分段带连接桩、溢出「+N 更多」、窄屏色点降级 |
| 周视图 | 每天一个孤立 chip | **week-card**：同一阶段跨多天连成一条（4px 项目色左边框），今天高亮克制，空列「无排期」 |
| 今日视图 | 普通列表 | **工作台三卡区**：今天要做 / 今天节点 / 今天编辑的便签 |
| 右侧详情 | 依赖弹窗 | **常驻右侧面板**：项目详情（状态/时间范围/当前阶段/下一节点/条目列表/关联便签/新增/编辑/归档）+ 手动添加/修正表单 + 后续导入提示 |
| 过期项目 | 只按 `status=active` 判断 | 生命周期派生 archived/ended/active：已结束项目移入次级「已结束待归档」行 + 归档按钮；已归档默认隐藏，「显示归档」开关可恢复 |
| 圆角 | 10px 卡片 | 统一 8px 半径，与 PinNote 现有卡片风格一致 |
| 手动闭环 | AI 入口与手动同层级 | 手动新增项目/新增排期/快速录入一周为主流程，AI 只做右侧底部导入提示 |

### 11.2 架构调整

1. **数据流改全量拉取**：`GET /api/work-schedule-items` 允许省略 `from/to` → 返回全部未删除排期；`fetchItems()` 无参拉全量。项目与排期各拉一次，三视图、右栏、指标共用同一份数据，客户端按视图裁剪。
2. **筛选状态上提 page.tsx**：`calendarHidden(Set)` + `calendarNoteLayer(boolean)` 由 `CalendarPageClient` 经 `onFilterChange` 回传；`CalendarCard` 今日预览按 `hiddenProjectIds` 过滤、按 `showNoteLayer` 显示链接角标。便签链接开关全局作用于月/周/今日 + 左侧预览卡。
3. **生命周期派生**：`lifecycleOf` 计算 `archived / ended（全部排期 endDate < 今天，待归档）/ active`。顶部 chip 只显示 active 未结束；ended 走次级行（「已结束」标签 + 归档按钮）；archived 默认不显示，开「显示归档」才出现（带恢复按钮）。月历历史仍可查看过期项目。
4. **月视图甘特层**：`ROW_H=104`、`LANE_TOP=28`、`LANE_H=26`、`MAX_LANES=3`；连续阶段跨周/跨月拆成段，段间以连接桩（`::before/::after`）衔接；溢出 →「+N 更多」；桌面显示甘特层、`<sm` 降级为色点。
5. **右栏重写**：`WorkRightPanel` 承担项目详情 + 手动添加/修正表单 + 导入提示；删除旧组件 `WorkProjectSidePanel` / `WorkScheduleChip` / `WorkScheduleItemDialog`；`WorkProjectDialog`（新增项目）与 `QuickWeekDialog`（快速录入一周）保留为弹窗，与右栏共用同一份数据。

### 11.3 改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `src/components/calendar/CalendarPageClient.tsx` | 三行头部（标题/说明/导航 + segmented control）、筛选行、指标行、生命周期派生、常驻右栏 + 窄屏抽屉；新增 props `hiddenProjectIds/showNoteLayer/onFilterChange` |
| 修改 | `src/components/calendar/work/WorkMonthView.tsx` | 月历格内轻量甘特层：定高格/3 分轨/跨周拆分/连接桩/溢出/窄屏色点 |
| 修改 | `src/components/calendar/work/WorkWeekView.tsx` | week-card 重排：跨多天连续、4px 项目色左边框、今天高亮、空列「无排期」、窄屏纵向列表 |
| 修改 | `src/components/calendar/work/WorkTodayView.tsx` | 今日工作台三卡区 + 完成勾选 + 便签链接打开 |
| 新增 | `src/components/calendar/work/WorkRightPanel.tsx` | 常驻右栏：项目详情（状态/时间范围/阶段/节点/条目/关联便签/归档）+ 手动添加/修正表单 + 导入提示 |
| 新增 | `src/components/calendar/work/color.ts` | `wpClass(colorKey)` 项目色系映射 |
| 删除 | `src/components/calendar/work/WorkProjectSidePanel.tsx` | 旧右侧项目面板（被 WorkRightPanel 取代） |
| 删除 | `src/components/calendar/work/WorkScheduleChip.tsx` | 旧排期 chip（被甘特条/week-card 取代） |
| 删除 | `src/components/calendar/work/WorkScheduleItemDialog.tsx` | 旧条目弹窗（被右栏表单取代） |
| 修改 | `src/components/calendar/CalendarCard.tsx` | 今日预览按 `hiddenProjectIds` 过滤 + `showNoteLayer` 链接角标 |
| 修改 | `src/components/NoteList.tsx` / `Sidebar.tsx` | 透传 `calendarHiddenProjectIds/calendarShowNoteLayer` |
| 修改 | `src/app/(protected)/page.tsx` | `calendarHidden`/`calendarNoteLayer` 状态上提 + `handleCalendarFilterChange` 回传 |
| 修改 | `src/hooks/useWorkCalendar.ts` | `fetchItems` 无参拉全量 |
| 修改 | `src/app/api/work-schedule-items/route.ts` | GET 允许省略 `from/to` |
| 修改 | `src/app/globals.css` | `wc-bar` 甘特样式、连接桩 `::before/::after`、`wp-*` 项目色系（含暗色变体） |

### 11.4 视觉验收（浏览器实测，dev，admin）

测试数据：本地 DB 造「M12 视觉验收」（蓝）项目 4 条排期（UI 改版走查 range 08-08→08-12、今日节点汇报 node 08-09 关联便签、视觉走查 range 08-10→08-13、线上评审 node 08-12 done）；「M12验证项目B（红）」全部排期 ≤08-07 派生为「已结束待归档」。

| 验收项 | 结果 |
|---|---|
| 三栏布局：左便签栏 + 日历主区 + 常驻 300px 右栏；右栏在窄屏（<sm）变抽屉 | ✅ |
| 顶部三行：标题/说明 + 今日/月/周 segmented control；筛选行含项目 chip + 便签链接开关 + 新增排期/项目/快速录入；指标行 4 格 | ✅ |
| 月视图：104px 定高格、甘特条落在格内、3 分轨并行、同项目同色系、连接桩衔接跨周拆分、溢出「+N 更多」 | ✅ |
| 周视图：week-card 跨多天连续（同阶段 8/3-8/4 两天各 1 张、4px 色左边框、今天高亮） | ✅ |
| 今日视图：今天要做/今天节点/今天编辑的便签三卡区；勾选完成切换 | ✅ |
| 点甘特条 → 右栏显示条目编辑；点项目 chip → 右栏显示项目详情 | ✅ |
| 筛选闭环：隐藏「M12 视觉验收」→ 指标 8→4、月视图甘特条 9→4、左侧预览卡同步；再显示恢复 8 项 | ✅ |
| 便签链接全局开关：关闭后月/周/今日链接层隐藏、指标「便签链接」归 0 | ✅ |
| 过期项目：红项目移入「已结束待归档」次级行 + 归档按钮；归档后默认隐藏，「显示归档」后可恢复 | ✅ |
| 暗色/亮色：`.wp-*` 色系变量随 `data-theme` 切换（#4276dc↔#5d8cff 等） | ✅ |
| 390px 窄屏：月视图降级为色点、周视图纵向列表、右栏抽屉、无横向溢出 | ✅ |
| 独立页 `/calendar`（管理员）：返回便签 Link + 完整功能仍可用 | ✅ |
| 普通用户：列表无日历分组、`/calendar` 重定向 + toast、`/api/calendar-events` 403 | ✅ |

### 11.5 截图归档（docs/img-m12-visual/）

QA §6 要求的交付证据，9 张 PNG（本地 dev 实测，headless Chrome CDP 捕获，亮/暗色 + 窄屏）：

| 文件 | 内容 |
|---|---|
| `01-month.png` | 月视图（亮色，甘特条 9 条） |
| `02-week.png` | 周视图（week-card，8/3-8/9） |
| `03-today.png` | 今日视图（三卡区工作台） |
| `04-month-rightpanel.png` | 月视图 + 右侧详情/编辑面板（选中条目） |
| `05-filter-off.png` | 隐藏「M12 视觉验收」项目后（指标与甘特同步下降） |
| `06-dark-month.png` | 暗色月视图 |
| `07-narrow-month.png` | 390px 窄屏月视图（色点降级） |
| `08-narrow-week.png` | 390px 窄屏周视图 |
| `09-standalone-calendar.png` | 独立页 `/calendar` |

### 11.6 构建与部署

- 本地 `npx tsc --noEmit`：通过；`npm run build -- --webpack`：通过。
- 生产部署：备份 `prod_M12_visual_20260809_221604.db`（356352 B）→ rsync `src/ --delete`（无 schema/根配置改动）→ 服务器 `rm -rf .next && npm run build -- --webpack`（14.8s 编译成功）→ 重启 `sticky-notes.service`（active）。
- 健康检查：`/login` 200、未登录 `/` 307→`/login`、`/api/auth/me` 未登录 401、admin 登录（`ding2860`）200、`GET /api/work-schedule-items` `{ok:true,items:[]}`、`GET /api/work-projects` `{ok:true}`（含既有项目「测试」）、`GET /api/calendar-events?from&to` `{ok:true}`、域名 `https://notes.hello-mylife.online/login` 200。
- 无 schema 改动、无 DB 迁移（数据模型未变，仅 API 允许省略参数 + 前端重排）。

### 11.7 未做项

- 不新增 AI 排期导入（仅右侧底部「后续导入」提示）。
- 冲突检测、拖拽排期、提醒通知不做（与 QA §3.2 一致）。
- 不改后端主模型（未新增字段，未改 Prisma schema）。

### 11.8 风险与后续建议

- **`WorkMonthView` 潜在 hook 违规**：`useMemo`（colors 计算）写在 `.map()` 回调内（文件约 131 行），因格数稳定目前不触发 React 规则错误，属技术债。建议后续把配色计算提到组件顶层，避免格数或渲染结构变化时踩 rules-of-hooks。**不阻塞本轮交付**。
- 筛选状态上提后由 page.tsx 持有，单栏切走日历再切回、或刷新页面时会重置为默认（全部显示 + 链接开），符合「无持久化筛选」的产品预期；如需记忆可在 M13 做 localStorage 持久化。
- `快速录入一周` 与 `WorkProjectDialog` 仍是弹窗形态，与右栏共用数据，符合原型「手动输入为主流程」的层级要求。
- Windows 桌面壳外链 P1（旧安装包缺 `open_external` 命令）维持 M12 记录，待重打 Windows 包验证，与本轮无关。

## 十二、QA 基础可用返工（2026-08-09，晚间第四轮）

按 `docs/QA_M12_工作日历基础可用返工_20260809.md`，选定「整体重做为基础原型」，以 `/Users/dian/.codex/visualizations/2026/08/06/019fd7e6-8a92-7780-80ce-ef4cfb9e710b/work-calendar-foundation-prototype.html` 为**唯一视觉与交互基准**。本轮验收重点不是功能数量，而是**基础日历样式 + 手动添加闭环真的能用**。不做快速录入主流程、不做 AI 导入、不做 Excel/PDF/图片解析。

### 12.1 返修结论

| 项 | 返修前 | 返修后 |
|---|---|---|
| 布局 | 三栏工作空间（左便签 + 日历主区 + 常驻 300px 右栏） | **双栏**：日历主区 + 右侧手动添加/编辑表单（窄屏抽屉） |
| 顶部区 | 三行（标题/说明 + 筛选/快捷入口 + 指标行 4 格） | 标题 + 说明 + 导航/今日-月-周 segmented control + 筛选行（项目 chip + 便签链接开关 + 添加连续阶段/添加单日节点/新增项目/显示归档） |
| 指标行 | 4 格指标（当前显示/连续阶段/单日节点/便签链接） | 删除 |
| 快速录入一周 | 弹窗入口（QuickWeekDialog） | 删除入口与组件 |
| 月视图 | 104px 定高格 + 3 分轨甘特 + 连接桩 + 溢出「+N」 | **106px 定高格完整月历**：连续阶段软底色条（20px）+ 单日节点虚线 + 便签绿色药丸；今天高亮；窄屏色点降级；**3 分轨兜底，第三重叠阶段不再被静默丢弃** |
| 周视图 | week-card 跨天连续 | **周一~周日 7 列**，每天显示对应排期（task-card 软底 + 4px 主色左边框），今天列高亮，空列「无排期」，窄屏纵向列表 |
| 今日视图 | 三卡区 + 完成勾选 | 三卡区：**今天要做 / 今天节点 / 今天编辑的便签** |
| 右侧表单 | 项目详情 + 排期表单 + 导入提示 | **纯手动添加/编辑表单**：标题 / 所属项目 / 类型（连续阶段\|单日节点）/ 起止日期 / 关联便签 / 保存 / 删除 |
| 入口卡 | 底部「+ 添加今日事件」 | 入口卡片（工作日历 + 今日未完成 N 项 + 最多 2 条预览），点击在右面板打开嵌入日历 |

### 12.2 架构调整

1. **数据流沿用全量拉取 + 客户端裁剪**：`fetchItems()` 无参拉全部未删除排期，月/周/今日与右栏共用同一份数据，各视图按需裁剪（沿用 11.2.1）。
2. **右栏纯表单化**：`WorkRightPanel` 删除项目详情/指标/导入提示，只保留手动添加/编辑表单（`startNewItem` 触发、`newItemNonce` 重置、乐观更新 + 失败回滚不变）。
3. **月历 3 分轨几何**：`ROW_H=106`、`LANE_TOPS=[34,56,78]`、`MAX_LANES=3`、`NOTE_TOP=84`（色条 20px）。便签药丸由排期条派生，只给 `lane<2` 且所在行未占满 3 轨的段，避免与第 3 轨叠压。
4. **删除快速录入**：删 `QuickWeekDialog.tsx`；`useWorkCalendar` 移除 `quickWeekPreview`；`CalendarPageClient` 移除指标行与新排期快捷入口。

### 12.3 改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `src/app/globals.css` | `.wc-bar` 20px 软底 + 主色细边；`.wc-node` 虚线单日节点；`.wc-note-pill` 18px 绿色药丸；`.wc-note-ico` |
| 修改 | `src/components/calendar/work/WorkMonthView.tsx` | 基础完整月历 + 格内色条/虚线节点/便签药丸；3 分轨 + 第 3 轨兜底 |
| 修改 | `src/components/calendar/work/WorkWeekView.tsx` | 周一~周日 7 列 task-card，今天列高亮，空列「无排期」，窄屏纵向 |
| 修改 | `src/components/calendar/work/WorkTodayView.tsx` | 三卡区 task-card（今天要做/今天节点/今天编辑的便签） |
| 修改 | `src/components/calendar/work/WorkRightPanel.tsx` | 纯手动添加/编辑表单 |
| 修改 | `src/components/calendar/CalendarCard.tsx` | 入口卡片：工作日历 + 今日未完成计数 + 预览；删除「+ 添加今日事件」 |
| 修改 | `src/components/calendar/CalendarPageClient.tsx` | 基础原型头部/工具栏；去指标行/去快速录入；右栏改表单；`embedded` 嵌入首页右面板形态保留 |
| 修改 | `src/hooks/useWorkCalendar.ts` | 移除 `quickWeekPreview` |
| 修改 | `src/app/(protected)/page.tsx` / `NoteList.tsx` / `Sidebar.tsx` | 移除 `onOpenCalendarNew` / `calendarNewEvent` 接线（入口仍在侧栏列表「日历」分组） |
| 删除 | `src/components/calendar/work/QuickWeekDialog.tsx` | 快速录入一周入口 |

### 12.4 验收（浏览器实测，dev，admin + 程序化几何断言）

测试数据：既有项目「M12 视觉验收」（蓝）4 条 +「M12验证项目B（红）」5 条；本轮新增测试条「A 修改」（连续阶段 08-10~08-13，与既有「视觉走查」同日重叠）与「A call」（单日节点 08-15，验收后删除）。

| 验收项 | 结果 |
|---|---|
| 月视图：49 格完整月历、今天高亮、连续阶段色条、单日节点虚线 | ✅ |
| 周视图：周一~周日 7 列等宽（实测 7 列 × 110px），每天显示对应排期，空列「无排期」 | ✅ |
| 今日视图：今天要做（UI 改版走查）/ 今天节点（今日节点汇报）/ 今天编辑的便签（2 条）三区内容正确 | ✅ |
| 手动添加连续阶段 → 真实写入 DB，月视图立即出现（`A 修改`） | ✅ |
| 手动添加单日节点 → 月视图出现虚线节点（`A call`） | ✅ |
| 修改结束日期 08-13→08-14 → 月/周同步更新 | ✅ |
| 删除 `A call` → 三视图同步消失 | ✅ |
| 刷新页面 → `A 修改` 仍在、`A call` 不再出现（持久化） | ✅ |
| **3 轨月历**：`A 修改` 与 `视觉走查`（08-10~08-13）同日重叠仍正确渲染在第 3 轨；CDP 几何断言 0 条叠压、0 药丸冲突 | ✅ |
| 便签链接开关：关闭后月视图药丸隐藏（1→0），色条不受影响 | ✅ |
| 项目筛选：隐藏「M12验证项目B（红）」→ 月视图色条 9→5 | ✅ |
| 390px 窄屏：无横向溢出（`scrollWidth == innerWidth == 390`） | ✅ |
| 暗色/亮色 token 随 `data-theme` 切换（`#4276dc↔#5d8cff`、`#18a06d↔#4dd7a8` 等） | ✅ |

### 12.5 截图归档（docs/img-m12-foundation/）

QA §3 要求的交付证据，10 张 PNG（本地 dev 实测，headless Chrome CDP 捕获，亮/暗色 + 窄屏）：

| 文件 | 内容 |
|---|---|
| `01-month.png` | 月视图（49 格，色条 + 单日节点 + 便签药丸） |
| `02-week.png` | 周视图（周一~周日 7 列） |
| `03-today.png` | 今日视图（三卡区） |
| `04-range-added.png` | 手动添加连续阶段 `A 修改` 后月视图 |
| `05-node-added.png` | 手动添加单日节点 `A call` 后月视图 |
| `06-today-synced.png` | 添加后今日视图同步 |
| `07-edit-synced.png` | 修改 `A 修改` 结束日期后 |
| `08-narrow-month.png` | 390px 窄屏月视图（色点降级） |
| `09-narrow-week.png` | 390px 窄屏周视图 |
| `10-dark-month.png` | 暗色月视图 |

### 12.6 构建与部署

- 本地 `npx tsc --noEmit`：通过；`npm run build -- --webpack`：通过。
- 生产部署：备份 DB → rsync `src/ --delete` → 服务器 `rm -rf .next && npm run build -- --webpack` → 重启 `sticky-notes.service` → 健康检查（详见下方部署记录）。
- 无 schema 改动、无 DB 迁移。

### 12.7 未做项

- 快速录入一周：本轮弱化/隐藏，不作为主流程（QA §3.3）。
- 不做 AI 导入、Excel/PDF/图片解析（QA §3.4）。
- 冲突检测、拖拽排期、提醒通知仍不做（与 QA §3.2 一致）。

### 12.8 风险与后续建议

- **月历 3 轨为硬上限**：同一天 4 个以上重叠阶段会从第 4 个起静默丢弃。本轮以「完整基础闭环」为验收目标，先保证 3 轨不丢；后续若数据密度上来，可加「+N 更多」溢出折叠（11.x 版本有该能力，本轮按原型去掉）。
- 便签药丸在行内占用第 4 层空间，仅在 lane<2 且行未占满 3 轨时显示，属有意的信息降级，避免与排期条叠压。
- 项目筛选/便签链接开关状态仍由 page.tsx 持有，切走再切回会重置为默认，符合「无持久化筛选」预期；如需记忆可在 M13 做 localStorage 持久化。

---

## 十三、QA 基础体验返修（2026-08-09 晚 ~ 08-10 凌晨）

按 `docs/QA_M12_工作日历基础体验返修_20260809.md` 做基础体验返修。QA 判定 M12 骨架已跑通但**基础体验不通过**：项目管理缺失、教学式文案过多、排期类型心智负担重、周/今日视图挤在顶部、日历→便签缺返回。本轮只修这 5 项基础体验，不扩 AI 导入、快速录入、拖拽等新功能。

### 13.1 返修结论

| 项 | 返修前 | 返修后 |
|---|---|---|
| 项目管理 | 只有「新建项目」，无编辑/归档/删除入口 | **项目详情右侧面板**：点项目 chip/名称 → 右栏显示项目详情（生命周期/时间范围/描述/N 条排期），底部 [＋添加排期 / 编辑项目 / 归档项目 / 删除项目]。编辑可改名称/颜色/时间；归档后移入归档行（默认隐藏，「显示归档」可恢复）；删除走 `confirm` 软删除（`deletedAt` 置位，刷新不再出现），默认操作优先归档、删除为二级危险操作 |
| 解释性文案 | 「添加连续阶段和单日节点，保存后写入数据库并在月/周/今日视图同步显示」等 | **全部删除**，页面只保留必要标签与状态；删 `SUB_COPY`，空态统一「暂无项目」「今天无排期。」 |
| 排期类型 | 表单显式选择「连续阶段 / 单日节点」 | **删除类型选择器**，表单只保留：标题/所属项目/开始日期/结束日期/状态/关联便签（可选）。保存时自动写：`startDate===endDate → type="node"`，`startDate<endDate → type="range"` |
| 周/今日排版 | 内容挤在顶部一小块 | **周视图撑满**：7 列等宽占满主区垂直高度（1440px 实测填充率 99%，底部仅留 12px）；每列内部卡片化留白。**今日视图工作台化**：今天要做/今天节点/今天编辑的便签三分区、`sm:grid-cols-2` 双栏铺满（1440px 宽度填充 96%） |
| 日历→便签返回 | 点击今日便签进入后无返回入口 | **保持挂载 + 返回工作日历**：日历层在打开便签后仍挂载（CSS 隐藏），顶部「返回工作日历」入口；点击后日历即时恢复、视图上下文与便签链接筛选不丢，不跳独立 `/calendar`、不开新页 |

### 13.2 架构调整

1. **`WorkRightPanel` 改为双模式**：新增 `detailProject/detailItemCount/detailLifecycle` + `onEditProject/onArchiveProject/onRestoreProject/onDeleteProject/onAddItemToProject` 五个回调；`detailMode = Boolean(detailProject && !selectedItem)` 渲染 `<ProjectDetail>`（项目详情）；表单模式不变（仅去类型选择器、新增状态字段）。保存时 `end = endDate || startDate` 后由日期相等性自动派生 `type`。
2. **`CalendarPageClient` 接线项目生命周期**：`rightMode: "item"|"project"` 控制右栏是排期表单还是项目详情；`selectProject(id)` 打开详情、`handleEditProject` 打开项目编辑弹窗、`handleArchiveProject/handleDeleteProject` 在项目从列表消失时复位 `selectedProjectId/rightMode/rightOpen`；删除 chip 内联归档/恢复按钮（只保留 已结束/已归档 标签），顶部「添加排期」合并为单按钮。
3. **周/今日视图撑满**：`WorkWeekView` 根节点 `hidden min-h-0 flex-1 flex-col sm:flex` + 主体 `grid min-h-0 flex-1 grid-cols-7`，7 列 `flex min-h-0 flex-col items-stretch gap-1.5 overflow-y-auto`；`WorkTodayView` 去 `max-w-[680px]` 约束，三分区 `sm:grid-cols-2` 双栏。
4. **日历保持挂载 + 返回**：`page.tsx` 新增 `cameFromCalendar`；`handleCalendarOpenNote` 打开便签时 `setCalendarOpen(false); setCameFromCalendar(true)`，日历层按 `isAdmin && (calendarOpen || cameFromCalendar)` 常驻挂载、CSS 切换可见性（`showCalendarPanel`）；`NoteEditor` 新增 `onCalendarReturn/calendarReturnLabel` props，顶部渲染「← 返回工作日历」条；`returnToCalendar` 恢复 `calendarOpen=true`。复用 M11 内链返回栈思路，不另起一套。

### 13.3 改动文件

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `src/components/calendar/work/WorkRightPanel.tsx` | 双模式重构：项目详情（`ProjectDetail`：生命周期/时间范围/描述/排期数/＋添加排期/编辑/归档或恢复/删除危险按钮）+ 表单去类型选择器、加状态字段、保存自动派生 type |
| 修改 | `src/components/calendar/CalendarPageClient.tsx` | 去 `SUB_COPY` 教学文案；`rightMode` 替代 `newPresetType`；`selectProject` 打开项目详情；编辑/归档/删除接线；chip 去内联归档按钮；「添加排期」合并单按钮；空态简化 |
| 修改 | `src/components/calendar/work/WorkWeekView.tsx` | 7 列撑满主区垂直空间、列内卡片化留白 |
| 修改 | `src/components/calendar/work/WorkTodayView.tsx` | 三分区工作台化、`sm:grid-cols-2` 双栏铺满、去教学子文案 |
| 修改 | `src/components/NoteEditor.tsx` | 新增 `onCalendarReturn/calendarReturnLabel`，顶部「返回工作日历」条 |
| 修改 | `src/app/(protected)/page.tsx` | `cameFromCalendar` 状态 + 日历常驻挂载（CSS 显隐）+ `returnToCalendar`/`handleCalendarOpenNote` 接线 |

### 13.4 验收（浏览器实测，dev，admin + 普通用户，程序化 DOM/几何断言）

测试数据：本轮新增「M12返修项目管理」（紫，编辑后改名「-改」，验收后删除）、「M12验证项目B（红）」等；另造「M12单日显示8-13」「M12单日今日8-10」验证单日节点显示。

**2.1 项目管理：**
| 验收项 | 结果 |
|---|---|
| 点项目 chip → 右栏项目详情：[＋添加排期/编辑项目/归档项目/删除项目/取消] + 「0 条排期」+ 时间范围 + 描述 | ✅ |
| 编辑项目弹窗预填名称 → 改名保存 → 列表与详情同步为「-改」 | ✅ |
| 归档项目 → active 筛选区消失；开「显示归档」→ 归档行 chip → 详情按钮含 [恢复项目] | ✅ |
| 恢复项目 → 回到 active 筛选区 | ✅ |
| 删除项目（confirm 覆盖确认）→ 软删，详情与列表消失，刷新不再出现 | ✅ |

**2.2 解释性文案：** 页面无「保存后写入数据库/三视图同步/先把基础日历样式做清楚」等教学文案；`hasTypeText/hasAddRange/hasAddNode/hasSubCopy` 全 false。

**2.3 无显式类型：**
| 验收项 | 结果 |
|---|---|
| 添加排期表单标签 = 标题/所属项目/开始日期/结束日期/状态/关联便签（可选），**无**类型选择器 | ✅ |
| 单日保存 → DB `type=node`；月视图虚线 `wc-node`、今日「今天节点」出现 | ✅ |
| 多日保存 → DB `type=range`；月视图软底色条 `wc-bar` | ✅ |

**2.4 周/今日排版：**
| 验收项 | 结果 |
|---|---|
| 1440px 周视图：7 列等宽（110px）、列高 725px、主体填充率 99%（底部仅 12px）、列内卡片留白 | ✅ |
| 1440px 今日视图：三分区 [今天要做/今天节点/今天编辑的便签]、2 组 `sm:grid-cols-2` 双栏、宽度填充 96% | ✅ |
| 390px 周/今日：`scrollWidth===innerWidth` 无横向溢出 | ✅ |
| 320px 周/今日：`scrollWidth===innerWidth` 无横向溢出（x=-8 元素为收起态侧栏图标，非溢出） | ✅ |

**2.5 日历→便签返回：**
| 验收项 | 结果 |
|---|---|
| 今日视图点便签「Debug test」→ URL 仍 `/`、编辑器顶部「返回工作日历」条 | ✅ |
| 返回前日历层保持挂载（width 0 隐藏，视图状态不丢） | ✅ |
| 点「返回工作日历」→ 日历即时恢复（width 672）、今日视图上下文保留、便签链接仍在 | ✅ |
| 不跳独立 `/calendar`、不开新页 | ✅ |

**普通用户：** 侧边栏无「日历」分组；`/calendar` 重定向回 `/` + toast；`/api/calendar-events` 403。

### 13.5 截图归档（docs/img-m12-r2/）

QA §4 要求的交付证据，11 张 PNG（本地 dev 实测，headless Chrome CDP 捕获，桌面 + 窄屏）：

| 文件 | 内容 |
|---|---|
| `1-week.png` | 1440px 周视图（7 列撑满，卡片化） |
| `2-project-detail.png` | 项目详情右栏（编辑/归档/删除按钮） |
| `3-add-form.png` | 添加排期表单（无类型选择器，含状态字段） |
| `3b-month-node-range.png` | 月视图：多日→连续色条 + 单日→虚线节点并存 |
| `3c-month-single-node.png` | 月视图：干净日期的单日虚线节点（8/13） |
| `4-today.png` | 1440px 今日视图（三分区双栏工作台） |
| `4b-today-node.png` | 今日视图「今天节点」显示单日节点 |
| `5-note-editor-return.png` | 便签编辑器顶部「返回工作日历」 |
| `6-returned-calendar.png` | 返回后的工作日历（今日视图上下文保留） |
| `7-narrow-week-390.png` | 390px 窄屏周视图 |
| `8-narrow-week-320.png` | 320px 窄屏周视图 |

### 13.6 构建

- 本地 `npx tsc --noEmit`：通过；`npm run build -- --webpack`：通过。

### 13.7 未做项

- 不扩 AI 导入、快速录入、拖拽、冲突检测、提醒通知（与 QA §3 一致）。
- 不在项目详情中做排期批量管理；项目删除确认文案按当前 `confirm()` 简化实现。

### 13.8 风险与后续建议

- **月历 3 轨硬上限（预存在，非本轮引入）**：同一天 4 个以上重叠阶段从第 4 个起静默丢弃（本轮实测 8/9 已有 3 条占满 3 轨，第 4 条不显示）。验收改为干净日期验证单日显示。后续数据密度上来可加「+N 更多」溢出折叠，或提高 MAX_LANES。
- 项目删除目前用系统 `confirm()`；如需产品化确认弹窗（含「该项目下排期也会从当前视图隐藏」提示文案），可后续把 `confirm` 换成项目专用 Dialog。
- 便签药丸在月历行内占用第 4 层，仅在 lane<2 且行未占满 3 轨时显示，属有意的信息降级（沿用 12.8）。


