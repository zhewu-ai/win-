# M4 部署报告

> 提交日期：2026-07-24

---

项目名称：全平台便签（Web MVP）

部署路径：`/home/ubuntu/projects/sticky-notes`

Git commit：N/A（项目初始化后首次上线，尚未建立提交历史）

服务名称：`sticky-notes.service`（systemd --user）

监听地址：`127.0.0.1:9002`

访问域名：`https://notes.hello-mylife.online`

Caddy 配置摘要：
```
notes.hello-mylife.online {
    encode zstd gzip
    request_body {
        max_size 10MB
    }
    reverse_proxy 127.0.0.1:9002
}
```

是否改动 Hermes 域名块：否（仅追加新域名块）

Hermes Backend 是否正常：是（127.0.0.1:8000 正常监听）

Hermes Dashboard 是否正常：是（127.0.0.1:9119 正常监听）

DATABASE_URL：`file:/home/ubuntu/projects/sticky-notes/data/prod.db`

UPLOAD_DIR：`/home/ubuntu/projects/sticky-notes/uploads`

图片上传大小限制：5 MB

单便签图片数量限制：12

是否需要 Basic Auth：否

---

## 健康检查结果

| 检查项 | 结果 |
|--------|------|
| HTTPS 域名访问 | 307 → /login ✔ |
| 本地 curl 127.0.0.1:9002 | 307 ✔ |
| 登录接口 | 200 ✔ |
| 未登录鉴权（/api/notes） | 401 ✔ |
| 未登录图片访问 | 401 ✔ |
| 非图片上传 | 400 ✔ |
| 超限图片上传（>5MB） | 400 ✔ |
| 第 13 张图片 | 400 ✔ |
| 路径穿越（已登录） | 403 ✔ |
| 路径穿越（未登录） | 401 ✔ |
| 新建便签 + 自动保存 | 200 ✔ |
| 新建待办便签 + 勾选 | 200 ✔ |
| 上传图片 + 鉴权访问 | 200 ✔ |
| 回收站 + 恢复 + 永久删除 | 200 ✔ |

---

systemd 状态：**active (running)**，无错误日志

Caddy 状态：**active (running)**，TLS 证书已获取（Let's Encrypt）

---

## 最近 100 行服务日志（摘要）

```
Jul 24 13:41:35 VM-0-11-ubuntu systemd[1231]: Started sticky-notes.service - Sticky Notes Web App.
Jul 24 13:41:35 VM-0-11-ubuntu npm[413473]: > cross-platform-notes@1.0.0 start
Jul 24 13:41:35 VM-0-11-ubuntu npm[413485]:   ▲ Next.js 14.2.35
Jul 24 13:41:35 VM-0-11-ubuntu npm[413485]:   - Local:        http://localhost:9002
Jul 24 13:41:35 VM-0-11-ubuntu npm[413485]:  ✓ Starting...
Jul 24 13:41:35 VM-0-11-ubuntu npm[413485]:  ✓ Ready in 336ms
```

无错误、无异常重启。

## 最近 100 行 Caddy 日志（摘要）

证书申请阶段（DNS 传播前）存在 NXDOMAIN 错误，DNS 生效后已成功获取证书：

```
Jul 24 13:50:22 ... "msg":"served key authentication","identifier":"notes.hello-mylife.online"
Jul 24 13:50:23 ... "msg":"served key authentication","identifier":"notes.hello-mylife.online"
Jul 24 13:50:30 ... "msg":"authorization finalized","identifier":"notes.hello-mylife.online","authz_status":"valid"
Jul 24 13:50:33 ... "msg":"certificate obtained successfully","identifier":"notes.hello-mylife.online"
```

证书获取后无新增错误。

---

## 备份方案

- 已创建手工备份：`/home/ubuntu/backups/sticky-notes-20260724134206.tar.gz`
- 备份内容：`.env`、`data/`、`uploads/`
- 建议备份频率：每日 1 次，保留最近 7 天
- 建议方案：crontab + tar 本地归档，可选异机 rsync

## 回滚方案

待回滚时可执行：

```bash
export XDG_RUNTIME_DIR="/run/user/$(id -u)"
systemctl --user stop sticky-notes.service
# 恢复代码、.env、data/、uploads/ 至上一版本
npm ci && npm run build
systemctl --user start sticky-notes.service
systemctl --user status sticky-notes.service --no-pager
```

如数据库 schema 有变更需在回滚前评估可逆性。部署前已备份 `.env`、`data/`、`uploads/` 及 Caddyfile。

---

## 剩余风险

1. **无 Git 历史**：服务器上代码目前没有 .git 目录，后续更新若使用 rsync 须注意排除 data/、uploads/、.env。建议在服务器初始化 Git 仓库指向代码源。
3. **移动端验收**：CSS 响应式布局已在 M2/M3 实现，建议在真实手机端验证 390px 宽度下的列表/编辑器切换和图片缩略图溢出。
4. **无监控告警**：目前无进程宕机自动通知。如需要可配置健康检查（如 curl 定时检测 + 通知）。
