# 全平台便签 M6 产品规格：分组待办与反馈处理流

## 1. 本轮目标

M6 目标是把当前“普通 checklist”升级为更适合真实工作反馈处理的“分组待办”，并修复待办/便签模式切换中的交互问题。

核心场景：

```text
用户收到一批反馈。
有些反馈要在 C4D 里处理，有些要在 AE 里处理。
用户希望按软件或工作区域分组管理。
勾选完成后，该项自动进入当前组的已完成区域。
```

本轮重点不是做复杂项目管理系统，而是让便签里的待办真正适合处理多条分组反馈。

## 2. 当前问题

### 2.1 待办完成后只是划线停留

当前勾选后，待办项只变成划线状态，仍停在原位置。实际处理大量反馈时，已完成项会干扰未完成项扫描。

期望：

- 勾选后自动移动到当前组的已完成区域。
- 未完成项始终优先显示在上方。
- 已完成项弱化显示，可折叠。

### 2.2 已勾选项附近不好继续新增

当前用户反馈：在打勾的待办状态下，无法自然新增文本，只能先切回普通便签模式，编辑完再切回待办模式。

期望：

- 只要在待办模式下，任何时候都可以新增待办项。
- 在已完成项里按 Enter，也应新增一条未完成项。
- 不允许要求用户先切回便签模式才能继续写。

### 2.3 待办切回便签出现中括号污染

当前从待办模式切回普通便签时，会出现：

```text
[ ] 任务
[x] 已完成任务
```

这属于格式污染。

期望：

- 切回普通便签时，正文应是干净文本。
- 不应出现 `[ ]`、`[x]`、`☐`、`☑` 等技术格式前缀。

### 2.4 图片按钮位置不符合使用逻辑

当前图片按钮始终在编辑区底部。图片是插入动作，不应藏在正文最下方。

期望：

- 图片上传入口移到顶部工具栏。
- 图片展示区域仍可在正文/待办内容下方。
- 用户不需要滚到底部才能添加图片。

## 3. 本轮范围

### 3.1 P0 必须完成

1. 修复待办切回便签的中括号污染。
2. 修复待办模式下无法自然新增文本的问题。
3. 勾选待办后自动移动到当前组已完成区域。
4. 支持同一便签内多个待办分组。
5. 每个分组有未完成和已完成两个区域。
6. 已完成区域可折叠/展开。
7. 图片上传按钮移到顶部工具栏。
8. 保持已有 M2/M3 功能不回退。
9. `npm run build` 通过。

### 3.2 P1 尽量完成

1. 分组上移/下移。
2. 组内未完成项上移/下移。
3. 快速输入时支持用空行或标题自动识别分组。
4. 从普通文本切待办时自动识别简单分组。

### 3.3 本轮不做

1. 不做无限多级嵌套。
2. 不做看板。
3. 不做标签系统。
4. 不做任务负责人、截止日期、提醒。
5. 不做每个任务单独挂图片。
6. 不做拖拽排序，除非实现成本很低且不影响稳定性。
7. 不做 AI 自动分类。

## 4. 信息结构

### 4.1 推荐数据结构

当前 `Note.checklistItems` 是 JSON 字符串，前端类型是数组。M6 建议先继续复用这个字段，不新增数据库表。

为了兼容旧数据，建议将 checklist item 扩展为：

```ts
interface ChecklistItem {
  id: string;
  text: string;
  checked: boolean;
  sortOrder: number;
  createdAt: string;
  updatedAt: string;

  groupId?: string;
  groupTitle?: string;
  completedAt?: string | null;
}
```

说明：

- `groupId`：所属分组 ID。
- `groupTitle`：可选，仅用于兼容简单实现；更推荐独立 group 元数据。
- `completedAt`：完成时间，用于已完成区域排序。

更完整的结构也可用：

```ts
interface ChecklistGroup {
  id: string;
  title: string;
  sortOrder: number;
  collapsedCompleted?: boolean;
}

interface GroupedChecklistData {
  version: 2;
  groups: ChecklistGroup[];
  items: ChecklistItem[];
}
```

技术部可二选一，但必须满足：

- 旧 checklist 数据可读取。
- 新 grouped checklist 数据可保存。
- 接口返回给前端后类型稳定。
- 不得破坏已有便签。

### 4.2 兼容策略

旧数据：

```json
[
  { "id": "1", "text": "任务 A", "checked": false },
  { "id": "2", "text": "任务 B", "checked": true }
]
```

升级显示时应自动放入默认分组：

```text
待办
  未完成
  - 任务 A

  已完成
  - 任务 B
```

默认分组标题建议：

```text
待办
```

不要要求用户手动迁移旧数据。

## 5. 页面和交互规格

### 5.1 待办模式整体结构

待办模式下应显示：

```text
分组标题
  未完成项
  添加待办入口
  已完成 N

分组标题
  未完成项
  添加待办入口
  已完成 N
```

示例：

```text
C4D
  ○ 调整模型比例
  ○ 修改材质
  ○ 导出新版本
  + 添加待办

  已完成 2

AE
  ○ 改字幕
  ○ 换转场
  + 添加待办

  已完成 1
```

### 5.2 分组创建

P0 至少提供一种创建分组方式：

方案 A：按钮创建

```text
+ 添加分组
```

点击后在当前便签末尾创建新分组，并自动聚焦分组标题。

方案 B：输入识别

普通文本切待办时：

```text
C4D
- 调整模型比例
- 修改材质

AE
- 改字幕
- 换转场
```

自动识别为两个分组。

P0 可先做方案 A，P1 再做方案 B。

### 5.3 分组标题

要求：

- 分组标题可编辑。
- 空分组标题显示 placeholder：`新分组`。
- 删除分组前需要确认。
- 删除分组会删除该组下所有待办项。
- 至少保留一个分组。

建议操作：

```text
分组标题        上移 下移 删除
```

如果为了简洁，分组操作可以 hover 后显示。

### 5.4 新增待办项

要求：

- 每个分组下都有 `+ 添加待办`。
- 在任意待办输入框按 Enter 新增下一条。
- 在已完成项输入框按 Enter，也新增一条未完成项。
- 新增项默认进入当前组的未完成区域末尾。
- 新增后自动聚焦新项。
- 空便签切待办后，必须有一个默认分组和一个可编辑空待办项。

### 5.5 勾选完成

当用户勾选未完成项：

1. `checked` 变为 `true`。
2. 写入 `completedAt`。
3. 该项从未完成区域移动到当前组的已完成区域。
4. 已完成区域默认显示标题 `已完成 N`。
5. 已完成项文字弱化，允许划线。

完成项排序：

- P0：按完成时间倒序或正序均可，但必须稳定。
- 建议：最新完成在已完成区域最上方。

### 5.6 取消完成

当用户取消勾选已完成项：

1. `checked` 变为 `false`。
2. `completedAt` 清空。
3. 该项回到当前组未完成区域末尾。
4. 自动保存。

### 5.7 已完成区域折叠

每个分组下显示：

```text
已完成 3
```

行为：

- 点击可展开/收起。
- 默认状态建议收起。
- 如果已完成数量为 0，不显示已完成区域标题。

展开后显示：

```text
✓ 已完成任务 1
✓ 已完成任务 2
```

### 5.8 模式切换：普通便签 -> 待办

从普通便签切到待办时：

1. 如果正文为空，创建默认分组和一个空待办项。
2. 如果正文有多行，每一行默认转换为一个未完成待办项。
3. 不应保留 `[ ]`、`[x]` 前缀。
4. 如果文本中已有 `[ ]` 或 `[x]`，应识别状态，但存储到 checklist item 后，展示文本必须去掉前缀。

示例：

```text
[ ] 调整模型
[x] 已导出
```

转换为：

```text
○ 调整模型
✓ 已导出
```

实际 item 文本：

```text
调整模型
已导出
```

### 5.9 模式切换：待办 -> 普通便签

从待办切回普通便签时，必须输出干净文本。

推荐格式：

```text
C4D
调整模型比例
修改材质

已完成
导出旧版本

AE
改字幕
换转场

已完成
输出预览
```

不允许输出：

```text
[ ] 调整模型比例
[x] 导出旧版本
☐ 改字幕
☑ 输出预览
```

验收标准：

- 切回普通便签后，正文中不包含 `[ ]` 或 `[x]`。
- 连续来回切换 5 次，不出现前缀累积。
- 已完成状态可以在文本分区中表达，但不能用技术前缀污染正文。

## 6. 图片按钮调整

### 6.1 上传入口

图片按钮应从底部附件区域移到顶部工具栏。

要求：

- 用户在编辑长便签时，不需要滚到底部才能上传图片。
- 顶部工具栏中的图片按钮点击后打开文件选择器。
- 保留多图上传能力。
- 上传中状态仍可见。
- 上传失败仍可提示。

### 6.2 展示区域

图片缩略图仍可显示在正文/待办内容下方。

要求：

- 如果没有图片，不显示底部空白附件区域。
- 如果有图片，底部显示缩略图网格。
- 删除图片逻辑不变。
- 图片预览逻辑不变。

### 6.3 组件建议

技术部可以将图片上传按钮能力拆出：

```text
ImageUploadButton
ImageAttachmentGrid
```

这样顶部工具栏负责上传入口，正文下方只负责展示缩略图。

## 7. 自动保存要求

M6 改动会增加 checklist 操作频率，自动保存必须稳定。

要求：

1. 编辑分组标题会自动保存。
2. 新增待办会自动保存。
3. 勾选/取消勾选会自动保存。
4. 折叠已完成区域如果写入数据，也要自动保存。
5. 切换便签前必须 flush pending save。
6. 保存失败时仍可重试。

## 8. 移动端要求

390px 移动端必须可用：

1. 分组标题不溢出。
2. 待办输入不被按钮挤压。
3. 分组操作按钮不常驻占空间，可放在菜单或 hover/点击后显示。
4. 图片按钮在顶部工具栏可点击。
5. 已完成区域可折叠/展开。
6. 列表/编辑切换仍保持干净。

## 9. 验收清单

技术部完成后必须逐项回报：

### 9.1 构建

```text
QA-M6-001 npm run build 通过
```

### 9.2 模式转换

```text
QA-M6-002 空便签切待办后，有默认分组和可编辑空待办项
QA-M6-003 普通文本多行切待办后，每行成为待办项
QA-M6-004 文本中含 [ ]/[x] 时切待办，状态识别但文本不保留前缀
QA-M6-005 待办切回便签后，不出现 [ ]/[x]
QA-M6-006 普通/待办连续切换 5 次，不出现前缀累积
```

### 9.3 新增和编辑

```text
QA-M6-007 待办模式下按 Enter 可新增下一条
QA-M6-008 已完成项中按 Enter，可在当前组未完成区新增一条
QA-M6-009 新增项后自动聚焦
QA-M6-010 删除空项后焦点回到合理位置
QA-M6-011 至少保留一个分组
QA-M6-012 分组标题可编辑并保存
```

### 9.4 分组

```text
QA-M6-013 可新增 C4D 分组
QA-M6-014 可新增 AE 分组
QA-M6-015 C4D 和 AE 下可分别新增待办
QA-M6-016 刷新后分组和组内待办仍保留
QA-M6-017 删除分组前有确认
```

### 9.5 完成流转

```text
QA-M6-018 勾选未完成项后，自动移动到本组已完成区域
QA-M6-019 已完成项弱化显示
QA-M6-020 已完成区域显示数量
QA-M6-021 已完成区域可折叠/展开
QA-M6-022 取消勾选后，回到本组未完成区域
QA-M6-023 C4D 完成项不会跑到 AE 分组
```

### 9.6 图片

```text
QA-M6-024 图片按钮在顶部工具栏可见
QA-M6-025 不滚到底部也能上传图片
QA-M6-026 一次选择 3 张图片后立即显示 3 张
QA-M6-027 无图片时底部不显示多余附件空区域
QA-M6-028 有图片时底部缩略图正常展示
QA-M6-029 图片预览和删除不回退
```

### 9.7 移动端

```text
QA-M6-030 390px 下分组待办可编辑
QA-M6-031 390px 下图片按钮可点击
QA-M6-032 390px 下已完成区域可折叠/展开
QA-M6-033 390px 下列表/编辑切换干净
QA-M6-034 390px 下无横向滚动
```

### 9.8 回归

```text
QA-M6-035 登录/退出正常
QA-M6-036 新建普通便签正常
QA-M6-037 自动保存正常
QA-M6-038 保存失败重试正常
QA-M6-039 搜索正常
QA-M6-040 回收站恢复正常
QA-M6-041 永久删除正常
QA-M6-042 未登录图片访问仍返回 401
QA-M6-043 路径穿越请求仍不得返回 200
```

## 10. 推荐实施顺序

建议技术部按以下顺序做：

1. 先修模式切换中括号污染。
2. 修 checklist 中 Enter 新增逻辑。
3. 在现有 checklist item 上加入 `groupId` / `completedAt` 兼容字段。
4. 实现默认分组。
5. 实现分组展示和添加分组。
6. 实现勾选后移动到已完成区域。
7. 实现已完成折叠。
8. 移动图片按钮到顶部工具栏。
9. 跑 M6 验收清单。
10. 回归 M2/M3 安全和图片能力。

## 11. 本轮完成标准

M6 通过标准：

1. 用户可以在一张便签内创建 `C4D`、`AE` 等分组。
2. 每个分组可以独立新增、完成、取消完成待办。
3. 勾选后自动进入本组已完成区域。
4. 已完成区域可折叠。
5. 待办模式下任何时候都可以继续新增文本。
6. 待办切回便签不再出现 `[ ]` / `[x]`。
7. 图片按钮不再固定在底部，而是在顶部工具栏可用。
8. 构建通过，M2/M3 关键功能无回退。

未满足以上任一关键项，M6 不通过。
