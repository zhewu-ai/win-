---
# M8R3 部署报告

> 提交日期：2026-08-04
> 提交：9d10827（fix(m8): 第三轮体验——删极小模式按钮/待办紧凑/pointer拖拽/小窗底部入口）
> 部署类型：增量更新（M8 第 3 批体验优化）到已在线的生产环境

---

## 部署概况

项目名称：全平台便签（Web MVP）

部署路径：`/home/ubuntu/projects/sticky-notes`

更新方式：rsync 增量同步（无 `--delete`，排除 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`.git/`、`src-tauri/`、`.github/`、`shell-static/`、`.claude/`、`.cargo/`、`target/`）

服务名称：`sticky-notes.service`（systemd --user）

监听地址：`127.0.0.1:9002`

访问域名：`https://notes.hello-mylife.online`

本次更新内容（M8 第 3 批，纯前端，无 schema 变更，无需 npm ci）：
- F01 删除「极小模式/悬浮窗」按钮（该按钮在 Windows App 中点击会崩溃，主窗本就可缩小，保留窗口置顶能力）
- F02 待办行紧凑化：单行待办约 28px、行距约 32px，小窗可显示 8–10 条短待办；已完成区同步紧凑
- F03 拖拽排序从 HTML5 DnD 改为 Pointer Events（WebView2 下 HTML5 DnD 不可靠），并放大拖拽手柄命中区
- F04 小窗（≤420px）顶部「新建/账户」入口移到左下角悬浮迷你工具栏，释放顶部空间

---

## 变更前准备

### 备份（M4 §11 强制）

已备份至：`/home/ubuntu/backups/sticky-notes-20260804030000`

| 内容 | 大小 |
|------|------|
| `.env` | ✔ |
| `data/`（prod.db） | ✔ |
| `uploads/` | ✔ |
| Caddyfile | ✔ |
| 合计 | 6.8 MB |

### rsync 预演（dry-run）

- 同步项：`src/components/` 下 ChecklistEditor / NoteEditor / NoteList / TopBar / AutoGrowTextarea（本轮提交 9d10827 改动的 5 个文件）
- 确认 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`src-tauri/` 等均未涉及

---

## 构建与重启

- 服务器 `npm run build` 成功
- `systemctl --user restart sticky-notes.service` → **active**
- package.json 未变更，未执行 npm ci

---

## 健康检查结果

| 检查项 | 结果 |
|--------|------|
| 本地 curl 127.0.0.1:9002 根路径 | 307 → /login ✔ |
| 本地 curl /api/notes（未登录） | 401 ✔ |
| HTTPS 根路径 | 307 → /login ✔ |
| 登录接口（正确凭据） | 200 ✔ |
| 登录接口（错误凭据） | 401 ✔ |
| 登录后 /api/notes（cookie 会话） | 200，返回真实生产数据（含待办 kind=todo/heading 字段） ✔ |

---

## Windows 安装包

- CI：GitHub Actions run 30875374386（Build Windows Installer）**success**
- Release v0.1.0 已替换为本轮新包：
  - `StickyNotes_0.1.0_x64-setup.exe`（2,291,109 B）
  - `StickyNotes_0.1.0_x64_en-US.msi`（3,481,600 B）
- 页面：https://github.com/zhewu-ai/win-/releases/tag/v0.1.0

---

## 服务与日志

systemd 状态：**active (running)**

---

## 回滚方案

```bash
export XDG_RUNTIME_DIR="/run/user/$(id -u)"
systemctl --user stop sticky-notes.service
# 恢复备份 /home/ubuntu/backups/sticky-notes-20260804030000 中的 .env、data/、uploads/、Caddyfile
# 代码回滚：需恢复旧版本 src/ 源码并重新构建
npm run build
systemctl --user start sticky-notes.service
```

注意：本次无 schema 变更，回滚不涉及数据库结构。

---

## 剩余风险

1. **无 Git 历史**：服务器上代码仍无 `.git` 目录，更新依赖 rsync。建议后续在服务器初始化 Git 仓库。
2. **F03 拖拽需 Windows 真机确认**：Mac 浏览器 Pointer Events 拖拽逻辑已验证正常（指示条 + 排序生效），但 WebView2 实机手感待确认。
3. **F02 紧凑密度需 Windows 小窗确认**：320px 宽窗口下密度目标（8–10 条短待办）已在浏览器视口验证，Windows 主窗同宽度下需再确认一次。
