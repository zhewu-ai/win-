---
# M8R2 部署报告

> 提交日期：2026-08-04
> 提交：5be883b（fix(m8): 体验优化第2批——待办换行/小标题/ACL权限/时间戳/右键稳定）
> 部署类型：增量更新（M8 第 2 批体验优化）到已在线的生产环境

---

## 部署概况

项目名称：全平台便签（Web MVP）

部署路径：`/home/ubuntu/projects/sticky-notes`

更新方式：rsync 增量同步（无 `--delete`，排除 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`.git/`、`src-tauri/`、`.github/`、`shell-static/`、`.claude/`、`.cargo/`、`target/`）

服务名称：`sticky-notes.service`（systemd --user）

监听地址：`127.0.0.1:9002`

访问域名：`https://notes.hello-mylife.online`

本次更新内容（M8 第 2 批，纯前端，无 schema 变更，无需 npm ci）：
- F01 待办行改 autosize textarea + `overflow-wrap:anywhere`，长文本 260/320px 自然换行
- F02 待办小标题 `kind=heading`（扁平行模型，含旧分组数据迁移）、统一"已完成"区
- F04 时间戳从顶部移到底部弱显示
- F06 右键菜单一次性定位，去掉旧位置闪烁
- （F03 为 src-tauri 侧 ACL 权限，属 Windows 壳，不随 Web 部署）

---

## 变更前准备

### 备份（M4 §11 强制）

已备份至：`/home/ubuntu/backups/sticky-notes-20260804015739`

| 内容 | 大小 |
|------|------|
| `.env` | ✔ |
| `data/`（prod.db） | ✔ |
| `uploads/` | ✔ |
| Caddyfile | ✔ |
| 合计 | 6.8 MB |

### rsync 预演（dry-run）

- 同步项：`src/components/` 下 ChecklistEditor / NoteEditor / NoteList / AutoGrowTextarea / ContextMenu、`src/lib/note-serializer.ts`、`src/types/index.ts`、`src/app/(protected)/archive/page.tsx` + 2 份本轮文档
- 确认 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`src-tauri/` 等均未涉及

---

## 构建与重启

- 服务器 `npm run build` 成功（14 条路由生成，含 `/notes/[id]/floating`）
- `systemctl --user restart sticky-notes.service` → **active**
- package.json 未变更，未执行 npm ci

---

## 健康检查结果

| 检查项 | 结果 |
|--------|------|
| 本地 curl 127.0.0.1:9002 根路径 | 307 → /login ✔ |
| 本地 curl /api/notes（未登录） | 401 ✔ |
| HTTPS 根路径 | 307 → /login ✔ |
| 登录接口（正确凭据） | 200 ✔ |
| 登录接口（错误凭据） | 401 ✔ |
| 登录后 /api/notes | 200，返回 5 条笔记 ✔ |

---

## Windows 安装包

- CI：GitHub Actions run 30839036669（Build Windows Installer）**success**
- Release v0.1.0 已替换为本轮新包：
  - `StickyNotes_0.1.0_x64-setup.exe`（2,289,290 B）
  - `StickyNotes_0.1.0_x64_en-US.msi`（3,481,600 B）
- 页面：https://github.com/zhewu-ai/win-/releases/tag/v0.1.0

---

## 服务与日志

systemd 状态：**active (running)**

---

## 回滚方案

```bash
export XDG_RUNTIME_DIR="/run/user/$(id -u)"
systemctl --user stop sticky-notes.service
# 恢复备份 /home/ubuntu/backups/sticky-notes-20260804015739 中的 .env、data/、uploads/、Caddyfile
# 代码回滚：需恢复旧版本 src/ 源码并重新构建
npm run build
systemctl --user start sticky-notes.service
```

注意：本次无 schema 变更，回滚不涉及数据库结构。

---

## 剩余风险

1. **无 Git 历史**：服务器上代码仍无 `.git` 目录，更新依赖 rsync。建议后续在服务器初始化 Git 仓库。
2. **F03 运行期 ACL 需 Windows 真机确认**：本轮新增了 `allow-*` 权限（cargo check 通过），但置顶/极小模式的真实调用需 Windows 实机验证。
3. **拖拽排序手感**：Mac 浏览器实测逻辑正常，Windows 实机拖拽手感待确认。
