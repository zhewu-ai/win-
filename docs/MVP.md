# 全平台便签 MVP 文档

## 1. 产品定位

做一个以 Web 为核心的私有同步便签工具。第一阶段只交付网页端，不做 Windows、Mac、iOS、Android 原生应用，但在数据模型、接口和前端布局上预留未来桌面套壳能力。

产品目标：

- 用户可以在任意设备浏览器中打开自己的便签。
- 用户可以快速记录待办、备注、临时想法。
- 数据保存在自有服务器中，并在多设备之间同步。
- Web 端可以作为未来 Windows/Mac 桌面壳的直接承载页面。

非目标：

- 第一版不做原生移动 App。
- 第一版不做 Windows/Mac 桌面壳。
- 第一版不做多人协作、共享便签、团队空间。
- 第一版不做复杂富文本编辑器。
- 第一版不做提醒、日历、推送、小组件。

## 2. 目标用户

当前按单用户私有使用设计，默认用户是项目所有者本人。

典型使用场景：

- 在电脑浏览器中记录当天待办。
- 在手机浏览器中查看或补充一条备注。
- 在不同设备之间同步临时记录。
- 把重要便签置顶，把完成或过期内容归档。

## 3. MVP 范围

### 3.1 必须交付

1. 登录
2. 便签列表
3. 新建便签
4. 编辑便签标题和正文
5. 自动保存
6. 删除便签
7. 归档便签
8. 置顶便签
9. 搜索便签
10. 便签颜色
11. 多设备浏览器同步
12. 移动端浏览器基础适配
13. 服务端部署

### 3.2 可以延后

1. 注册系统
2. 邮箱找回密码
3. 实时 WebSocket 同步
4. 富文本编辑
5. Markdown 预览
6. 附件和图片
7. 标签系统
8. 回收站
9. 版本历史
10. 桌面悬浮窗

### 3.3 明确不做

1. 多用户共享
2. 公开发布平台
3. App Store / Microsoft Store 上架
4. Windows 托盘和快捷键
5. macOS 菜单栏
6. Android/iOS 原生功能

## 4. 第一版信息架构

页面结构：

- 登录页
- 便签主页
- 归档页

便签主页包含：

- 顶部搜索框
- 新建便签按钮
- 置顶便签区
- 普通便签区
- 便签编辑面板

移动端结构：

- 列表优先
- 点击便签进入编辑页或编辑面板
- 返回列表后继续保持筛选和搜索状态

## 5. 核心体验要求

### 5.1 记录要快

用户进入主页后，应能在 1 次点击内创建新便签并开始输入。

### 5.2 自动保存要可信

用户输入内容后不需要手动点击保存。系统需要显示保存状态：

- 保存中
- 已保存
- 保存失败

保存失败时不能静默丢失用户输入。

### 5.3 多设备同步要清楚

第一版不要求毫秒级实时同步，但要求：

- 刷新页面后能看到其他设备保存的最新内容。
- 切回页面时自动拉取最新便签。
- 同一条便签被不同设备编辑时，后保存的版本生效，并更新 `updatedAt`。

### 5.4 为桌面壳预留

Web 页面需要能在小窗口中使用，便签编辑区在窄窗口下不能依赖大屏布局。

数据模型需要预留桌面窗口相关字段，但第一版不展示这些字段：

- `windowX`
- `windowY`
- `windowWidth`
- `windowHeight`
- `alwaysOnTop`

## 6. 数据对象

### 6.1 User

- `id`
- `username`
- `passwordHash`
- `createdAt`
- `updatedAt`

第一版可以只创建一个管理员用户，不开放注册。

### 6.2 Note

- `id`
- `userId`
- `title`
- `content`
- `color`
- `isPinned`
- `isArchived`
- `sortOrder`
- `windowX`
- `windowY`
- `windowWidth`
- `windowHeight`
- `alwaysOnTop`
- `createdAt`
- `updatedAt`
- `deletedAt`

删除可以第一版直接软删除，设置 `deletedAt`。

## 7. MVP 验收标准

MVP 完成需要满足：

1. 用户可以通过域名打开 Web 端。
2. 用户可以登录。
3. 用户可以创建便签。
4. 用户输入标题或正文后，系统自动保存。
5. 用户刷新页面后内容仍然存在。
6. 用户在另一台设备或另一个浏览器中登录后，可以看到同一批便签。
7. 用户可以置顶便签，置顶便签显示在普通便签前。
8. 用户可以归档便签，归档后不再出现在主页。
9. 用户可以进入归档页查看归档便签。
10. 用户可以删除便签，删除后主页和归档页都不再显示。
11. 用户可以搜索标题和正文。
12. 用户可以给便签选择颜色。
13. 手机浏览器可以完成查看、新建、编辑、搜索。
14. 自动保存失败时界面有明确失败状态。
15. 服务端重启后数据不丢失。

## 8. 里程碑

### M1：基础可运行

- 项目初始化
- 登录能力
- 数据库初始化
- 便签 CRUD API
- 前端主页和编辑面板

验收：本地环境可以登录并创建、编辑、删除便签。

### M2：同步和状态

- 自动保存
- 保存状态展示
- 页面切回刷新
- 置顶、归档、颜色
- 搜索

验收：两个浏览器窗口之间可以通过保存和刷新看到同步结果。

### M3：移动端和部署

- 移动端适配
- 生产环境配置
- 域名和 HTTPS
- 数据库持久化
- 基础日志

验收：通过正式域名访问，手机和电脑浏览器均可正常使用。

## 9. 推荐技术边界

技术部可以自行选型，但需要满足：

- 前后端通过 API 通信。
- 核心数据不依赖浏览器本地存储。
- 认证机制未来可被桌面壳复用。
- API 路径保持稳定，未来 Windows/Mac 壳直接复用。
- 页面布局支持窄窗口。

推荐起步方案：

- 前端：React / Next.js
- 后端：Next.js API 或 Node.js 服务
- 数据库：SQLite 起步，后续可迁移 PostgreSQL
- 部署：Linux 服务器 + Nginx + HTTPS

