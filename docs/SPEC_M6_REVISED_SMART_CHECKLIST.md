# 全平台便签 M6 修订版：智能待办输入流与轻量完成区

## 1. 修订背景

原 M6 方向强调“分组待办”。实际体验后发现，显式分组过重，容易让产品变成项目管理工具，而不是顺手的备忘录。

用户的真实流程更接近：

```text
先把一批反馈复制进来
系统自动拆成多条待办
用户逐条处理
完成项自动下沉
后续再按需要整理
```

而不是：

```text
先建 C4D 分组
再建 AE 分组
再一条条录入
再处理
```

因此，本轮 M6 需要从“强分组”修订为：

```text
智能粘贴 + 快速新增 + 完成下沉 + 低闪烁 + 轻量整理
```

## 2. 本轮产品目标

M6 修订版目标：

1. 待办输入要像备忘录一样顺手。
2. 用户可以直接粘贴一整段反馈，系统自动拆成多条待办。
3. 完成项自动下沉，不干扰未完成项。
4. 编辑、勾选、切换便签时不闪烁。
5. 便签颜色要真正影响列表项视觉，而不是只改变小色条。
6. 暂停强化显式分组，先把单层待办流做好。

## 3. 当前问题

### 3.1 操作时随机闪烁

现象：

- 切换不同便签时页面闪一下。
- 在便签里新增待办时闪一下。
- 勾选待办时闪一下。
- 点击“添加待办”时也可能闪一下。

判断：

大概率是前端每次小操作后触发全量 `fetchNotes`，导致列表和编辑器一起重新渲染。

期望：

- 编辑当前便签、勾选待办、新增待办时，优先做本地状态更新。
- 后台静默保存。
- 不要每次小操作都刷新整个便签列表。
- 列表摘要可以局部更新，但编辑器不能闪。

### 3.2 颜色只影响小色条

现象：

- 便签有颜色选项。
- 但列表中大块选中背景始终是黄色。
- 用户感觉“颜色选择没有真正生效”。

期望：

- 便签颜色应同时影响：
  - 左侧色条
  - 列表项底色或选中态 tint
  - 必要时编辑区轻微 tint
- 不能只有小色条变化。
- 选中态仍需清楚，但不应永远覆盖成同一种黄色。

### 3.3 添加待办依赖按钮，输入不自然

现象：

- 用户感觉必须点“添加待办”。
- 这不像备忘录或 checklist 的自然输入。

期望：

- Enter 就是新增下一条。
- 不应强依赖“添加待办”按钮。
- “添加待办”可以保留为弱入口，但不是主路径。

### 3.4 粘贴多行反馈只变成一条

现象：

从外部复制：

```text
1. 修改模型比例
2. 材质太亮
3. AE 字幕错位
4. 输出一版预览
```

当前可能粘成一个待办项。

期望：

粘贴后自动变成 4 条待办。

### 3.5 序号和项目符号未清理

期望支持识别并清理：

```text
1. 任务
1、任务
1) 任务
A. 任务
A、任务
- 任务
* 任务
• 任务
○ 任务
☐ 任务
[ ] 任务
[x] 任务
```

最终待办文本不应保留这些前缀。

### 3.6 强分组过重

现象：

- “添加分组”显得太重。
- 分组区域挤压内容。
- 用户初始复制反馈时并不知道哪些属于 C4D，哪些属于 AE。

期望：

- 本轮暂停强化显式分组。
- 默认先是一个自然待办列表。
- 后续整理能力可以轻量保留，但不作为主交互。

## 4. 本轮范围

### 4.1 P0 必须完成

1. 修复待办操作闪烁。
2. 待办模式下 Enter 直接新增下一条。
3. 粘贴多行文本自动拆成多条待办。
4. 粘贴时自动清理序号、项目符号和 `[ ]/[x]` 前缀。
5. 勾选后自动移动到“已完成”区域。
6. 已完成区域可折叠/展开。
7. 取消勾选后回到未完成区域。
8. 待办切回便签时，不出现 `[ ]/[x]` 中括号污染。
9. 便签颜色影响列表项整体视觉，不只是小色条。
10. 图片按钮从底部移到顶部工具栏。
11. 暂停或弱化显式“添加分组”入口。
12. `npm run build` 通过。

### 4.2 P1 尽量完成

1. 支持通过缩进或空行轻量识别“可能的分段”，但不要强制分组。
2. 支持简单上移/下移排序。
3. 支持从已完成区按 Enter 新增未完成项。
4. 粘贴时识别 `A/B/C/D` 编号。

### 4.3 本轮不做

1. 不做强制 C4D/AE 分组。
2. 不做多级分组。
3. 不做看板。
4. 不做拖拽排序。
5. 不做 AI 分类。
6. 不做每个任务项单独挂图片。
7. 不做项目管理字段，如负责人、截止日期、优先级。

## 5. 推荐最终交互

### 5.1 默认待办结构

默认只显示：

```text
○ 修改模型比例
○ 材质太亮
○ AE 字幕错位
○ 输出一版预览

已完成 2
```

展开已完成后：

```text
已完成 2
✓ 旧版本已导出
✓ 字幕已替换
```

不要默认显示：

```text
添加分组
分组标题
C4D
AE
```

除非用户明确进入整理能力。

### 5.2 新增待办

行为：

- 在未完成项中按 Enter：在当前项下方新增一条未完成项。
- 在最后一条未完成项按 Enter：新增到未完成列表末尾。
- 在空待办项按 Enter：如果是最后一条，可保持一个空项，不制造大量空行。
- 新增后自动聚焦新项。

弱入口：

- 可以保留 `+ 添加待办`。
- 但视觉上要弱，不应成为唯一新增方式。

### 5.3 粘贴多行

用户在任意待办输入框粘贴多行时：

1. 阻止默认粘贴成一个 input value。
2. 按换行拆分。
3. 去掉空行。
4. 清理序号/项目符号前缀。
5. 保留 `[x]`、`☑` 等完成状态信息。
6. 将结果插入到当前项位置之后。
7. 如果当前项为空，可用第一条覆盖当前项，其余插入后面。
8. 粘贴完成后聚焦最后一条或下一条空项。

示例输入：

```text
1. 修改模型比例
2. 材质太亮
3. AE 字幕错位
4. 输出一版预览
```

结果：

```text
○ 修改模型比例
○ 材质太亮
○ AE 字幕错位
○ 输出一版预览
```

示例输入：

```text
[x] 已导出旧版
[ ] 修改字幕
☑ 已确认镜头
☐ 调整灯光
```

结果：

```text
○ 修改字幕
○ 调整灯光

已完成 2
✓ 已导出旧版
✓ 已确认镜头
```

### 5.4 完成下沉

勾选未完成项：

1. 立即从未完成区移除。
2. 进入已完成区。
3. 已完成计数更新。
4. 页面不闪烁。
5. 保存状态变成“保存中”，保存成功后弱化为“已保存”。

取消勾选：

1. 从已完成区移回未完成区末尾。
2. 页面不闪烁。
3. 自动保存。

### 5.5 已完成区域

要求：

- 已完成数量为 0 时不显示。
- 默认收起。
- 显示为：

```text
已完成 3
```

- 点击展开/收起。
- 展开后完成项弱化、可编辑、可取消勾选。

### 5.6 待办切回便签

切回普通便签时输出干净文本：

```text
修改模型比例
材质太亮
AE 字幕错位
输出一版预览

已完成
已导出旧版
已确认镜头
```

禁止输出：

```text
[ ] 修改模型比例
[x] 已导出旧版
☐ AE 字幕错位
☑ 已确认镜头
```

连续切换 5 次也不得产生前缀累积。

## 6. 闪烁修复要求

### 6.1 不要全量刷新

以下操作不应触发全量列表刷新：

```text
编辑标题
编辑正文
编辑待办文本
新增待办
勾选待办
取消勾选
展开/收起已完成
修改颜色
```

推荐实现：

- `NoteEditor` 内先更新本地 note state。
- `useNotes` 提供局部更新方法，例如 `updateNoteInList(note)`。
- PATCH 成功后只 reconcile 当前 note。
- 不要每次 `onUpdate` 都执行 `fetchNotes`。

允许全量刷新：

```text
进入页面初始化
搜索
删除
恢复
归档
切换归档/回收站页面
服务端保存失败后手动重试
```

### 6.2 验收标准

人工验收时观察：

- 勾选待办，编辑区不闪白、不重置焦点。
- 新增待办，列表不整块闪。
- 切换便签，只有内容自然切换，不出现明显闪屏。
- 保存状态变化不导致工具栏跳动。

## 7. 颜色策略

### 7.1 当前问题

如果所有选中便签都是同一块黄色，用户无法感知自己选择的便签颜色。

### 7.2 推荐方案

列表项颜色由便签颜色影响整体 tint：

```text
yellow: 选中背景 #A98A24，非选中 hover tint #3A311C
blue:   选中背景 #315D8F，非选中 hover tint #202B3A
green:  选中背景 #2F6E45，非选中 hover tint #1F3025
pink:   选中背景 #7A3F54，非选中 hover tint #36242B
gray:   选中背景 #5B5754，非选中 hover tint #2D2A29
```

同时保留左侧细色条，但细色条不再是唯一颜色表达。

要求：

- 选中态清楚。
- 字体对比度足够。
- 不要高饱和刺眼。
- 不要所有选中项都固定黄色。

## 8. 图片按钮调整

图片上传入口移到顶部工具栏。

要求：

- 不滚到底部也能上传图片。
- 原有多图上传能力保留。
- 图片展示区仍放在内容下方。
- 无图片时不显示多余附件空区域。
- 上传失败仍显示错误。

建议拆组件：

```text
ImageUploadButton
ImageAttachmentGrid
```

## 9. 弱化分组

### 9.1 本轮处理方式

如果当前已经实现了分组：

- 不要把“添加分组”放在主视觉位置。
- 默认不要求用户先选或新建分组。
- 可以保留一个“整理”或“更多”入口，放到次级菜单。
- 默认所有粘贴进来的反馈进入主待办列表。

### 9.2 后续可探索

后续如果要做更聪明的整理，可以考虑：

1. 用户选中多条待办后移动到分组。
2. 用轻量标签表示 `C4D` / `AE`。
3. 支持输入 `#C4D` 自动变标签。
4. AI 或规则建议分类，但用户确认后才应用。

这些不属于本轮 P0。

## 10. 数据结构建议

尽量继续复用 `checklistItems` JSON，减少迁移风险。

建议扩展：

```ts
interface ChecklistItem {
  id: string;
  text: string;
  checked: boolean;
  sortOrder: number;
  createdAt: string;
  updatedAt: string;
  completedAt?: string | null;
}
```

如果已有分组字段，可以保留但不强依赖：

```ts
groupId?: string;
groupTitle?: string;
```

要求：

- 旧数据可读。
- 新数据可存。
- 缺少 `completedAt` 的已完成旧项也能显示在已完成区。
- API 序列化不报错。

## 11. 验收清单

### 11.1 构建

```text
QA-M6R-001 npm run build 通过
```

### 11.2 闪烁

```text
QA-M6R-002 勾选待办时编辑区不闪烁
QA-M6R-003 新增待办时编辑区不闪烁
QA-M6R-004 修改待办文字时列表不整块闪烁
QA-M6R-005 修改颜色时当前便签不丢焦点
QA-M6R-006 切换便签时没有明显白屏闪动
```

### 11.3 输入和粘贴

```text
QA-M6R-007 待办模式按 Enter 新增下一条
QA-M6R-008 新增后自动聚焦新项
QA-M6R-009 粘贴 4 行纯文本，生成 4 条待办
QA-M6R-010 粘贴 1. 2. 3. 4. 编号文本，生成 4 条待办并清理编号
QA-M6R-011 粘贴 A. B. C. 编号文本，生成 3 条待办并清理编号
QA-M6R-012 粘贴 -/*/• 项目符号文本，生成多条待办并清理符号
QA-M6R-013 粘贴 [x]/[ ] 文本，识别完成状态但文本不保留前缀
QA-M6R-014 在空待办中粘贴多行，第一行覆盖当前项，其余插入后方
```

### 11.4 完成区

```text
QA-M6R-015 勾选未完成项后自动进入已完成区
QA-M6R-016 已完成区显示数量
QA-M6R-017 已完成区默认收起
QA-M6R-018 点击已完成区可展开
QA-M6R-019 已完成项可取消勾选并回到未完成区
QA-M6R-020 已完成项可编辑
```

### 11.5 模式切换

```text
QA-M6R-021 空便签切待办后有一个可编辑空项
QA-M6R-022 普通多行文本切待办后生成多条待办
QA-M6R-023 待办切回便签后不出现 [ ]/[x]
QA-M6R-024 普通/待办连续切换 5 次不出现前缀累积
```

### 11.6 颜色

```text
QA-M6R-025 选择 yellow 时列表项整体有 yellow tint
QA-M6R-026 选择 blue 时列表项整体有 blue tint
QA-M6R-027 选择 green 时列表项整体有 green tint
QA-M6R-028 选择 pink 时列表项整体有 pink tint
QA-M6R-029 选择 gray 时列表项整体有 gray tint
QA-M6R-030 选中态不是所有颜色都固定同一黄色
```

### 11.7 图片

```text
QA-M6R-031 图片按钮位于顶部工具栏
QA-M6R-032 不滚到底部也能上传图片
QA-M6R-033 一次选择 3 张图片后立即显示 3 张
QA-M6R-034 无图片时底部不显示空附件区域
QA-M6R-035 图片预览和删除功能不回退
```

### 11.8 移动端

```text
QA-M6R-036 390px 下待办输入可用
QA-M6R-037 390px 下粘贴多行可拆分
QA-M6R-038 390px 下已完成区可折叠/展开
QA-M6R-039 390px 下图片按钮可点击
QA-M6R-040 390px 下无横向滚动
```

### 11.9 回归

```text
QA-M6R-041 登录/退出正常
QA-M6R-042 新建普通便签正常
QA-M6R-043 自动保存正常
QA-M6R-044 保存失败重试正常
QA-M6R-045 搜索正常
QA-M6R-046 删除进入回收站正常
QA-M6R-047 回收站恢复正常
QA-M6R-048 永久删除正常
QA-M6R-049 未登录图片访问仍返回 401
QA-M6R-050 路径穿越请求仍不得返回 200
```

## 12. 推荐实施顺序

1. 先修 `onUpdate` 全量刷新导致的闪烁。
2. 修待办 Enter 新增。
3. 实现粘贴多行拆分。
4. 实现序号/项目符号清理。
5. 实现完成下沉和已完成折叠。
6. 修待办切回便签的 `[ ]/[x]` 污染。
7. 调整颜色策略，让列表项整体跟随便签颜色。
8. 移动图片按钮到顶部工具栏。
9. 弱化或隐藏显式添加分组入口。
10. 跑完整 M6R 验收清单。

## 13. 本轮通过标准

M6 修订版通过标准：

1. 操作待办时不再随机闪烁。
2. 粘贴多行反馈可以自动拆成多条待办。
3. 常见序号和项目符号会被清理。
4. Enter 新增自然，不依赖按钮。
5. 完成项自动下沉到已完成区。
6. 已完成区可折叠。
7. 待办切回便签无中括号污染。
8. 便签颜色影响列表项整体视觉。
9. 图片按钮位于顶部工具栏。
10. 强分组不再作为主交互。
11. M2/M3 核心功能无回退。

未满足以上任一关键项，M6 修订版不通过。
