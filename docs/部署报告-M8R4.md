---
# M8R4 部署报告

> 提交日期：2026-08-04
> 提交：ec7fc7b（fix(m8): 视觉第三轮——编辑态窄窗隐藏顶栏/小标题线字线居中/时间戳贴底）
> 部署类型：增量更新（M8 第 4 批，对应 QA_M8_ROUND3_VISUAL_FEEDBACK）到已在线的生产环境

---

## 部署概况

项目名称：全平台便签（Web MVP）

部署路径：`/home/ubuntu/projects/sticky-notes`

更新方式：rsync 增量同步（无 `--delete`，排除 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`.git/`、`src-tauri/`、`.github/`、`shell-static/`、`.claude/`、`.cargo/`、`target/`）

服务名称：`sticky-notes.service`（systemd --user）

监听地址：`127.0.0.1:9002`

访问域名：`https://notes.hello-mylife.online`

本次更新内容（M8 第 4 批，纯前端，无 schema 变更，无需 npm ci）：
- F01 窄窗编辑态隐藏全局顶栏空壳（≤420px），编辑区净增 56px；列表态搜索栏保留
- F02 待办小标题改为"线-字-线"居中样式（左右低透明横线 + 居中标题），320px 长标题不溢出
- F03 时间戳改为编辑区底部 footer：内容短时贴底、内容长时跟随内容滚动可见

---

## 变更前准备

### 备份（M4 §11 强制）

已备份至：`/home/ubuntu/backups/sticky-notes-20260804120632`

| 内容 | 大小 |
|------|------|
| `.env` | ✔ |
| `data/`（prod.db） | ✔ |
| `uploads/` | ✔ |
| Caddyfile | ✔ |
| 合计 | 6.8 MB |

### rsync 预演（dry-run）

- 同步项：`src/components/TopBar.tsx`、`src/components/ChecklistEditor.tsx`、`src/components/NoteEditor.tsx`（提交 ec7fc7b 改动的 3 个文件）
- 服务器哈希已与本地逐一比对，完全一致
- 确认 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`src-tauri/` 等均未涉及

---

## 构建与重启

- 服务器 `npm run build` 成功（14 条路由生成，含 `/notes/[id]/floating`）
- `systemctl --user restart sticky-notes.service` → **active**
- package.json 未变更，未执行 npm ci

---

## 健康检查结果

| 检查项 | 结果 |
|--------|------|
| 本地 curl 127.0.0.1:9002 根路径 | 307 → /login ✔ |
| 本地 curl /api/notes（未登录） | 401 ✔ |
| 登录接口（错误凭据） | 401 ✔ |
| 登录接口（正确凭据） | 200 ✔ |
| 登录后 /api/notes（cookie 会话） | 200，返回真实生产数据 ✔ |

---

## Windows 安装包

- CI：GitHub Actions run 30876663769（Build Windows Installer）**success**
- Release v0.1.0 已替换为本轮新包：
  - `StickyNotes_0.1.0_x64-setup.exe`（2,291,655 B）
  - `StickyNotes_0.1.0_x64_en-US.msi`（3,481,600 B）
- 页面：https://github.com/zhewu-ai/win-/releases/tag/v0.1.0

---

## 服务与日志

systemd 状态：**active (running)**

---

## 回滚方案

```bash
export XDG_RUNTIME_DIR="/run/user/$(id -u)"
systemctl --user stop sticky-notes.service
# 恢复备份 /home/ubuntu/backups/sticky-notes-20260804120632 中的 .env、data/、uploads/、Caddyfile
# 代码回滚：需恢复旧版本 src/ 源码并重新构建
npm run build
systemctl --user start sticky-notes.service
```

注意：本次无 schema 变更，回滚不涉及数据库结构。

---

## 剩余风险

1. **无 Git 历史**：服务器上代码仍无 `.git` 目录，更新依赖 rsync。建议后续在服务器初始化 Git 仓库。
2. **视觉项需 Windows 真机最终确认**：顶部空壳释放、小标题线-字-线居中、时间戳贴底三项已在 320px 浏览器视口以 DOM 几何测量验证，Windows 主窗同宽度下建议按验收指引再走一遍。
