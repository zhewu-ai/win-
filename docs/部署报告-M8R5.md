---
# M8R5 部署报告

> 提交日期：2026-08-06
> 提交：c27e7a2（fix(m8): 桌面质感第4批——全局入口下沉侧栏底/工具栏单行更多菜单/列表具体日期/侧栏折叠/自定义删除确认）
> 部署类型：增量更新（M8 第 5 批，对应 QA_M8_ROUND4_POLISH_FEEDBACK）到已在线的生产环境

---

## 部署概况

项目名称：全平台便签（Web MVP）

部署路径：`/home/ubuntu/projects/sticky-notes`

更新方式：rsync 增量同步（无 `--delete`，排除 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`.git/`、`src-tauri/`、`.github/`、`shell-static/`、`.claude/`、`.cargo/`、`target/`）

服务名称：`sticky-notes.service`（systemd --user）

监听地址：`127.0.0.1:9002`

访问域名：`https://notes.hello-mylife.online`

本次更新内容（M8 第 5 批，纯前端，无 schema 变更，无需 npm ci）：
- F01 移除全局顶栏，新建 + 账号/我的 下沉到侧边栏底部固定区，搜索留在侧栏顶部（TopBar → Sidebar）
- F02 编辑器工具栏恒单行：去掉 flex-wrap，置顶/归档/删除进「⋯」更多菜单，260px 不再换行
- F03 列表与回收站显示具体日期：新增 lib/format-time（今天 14:30 / 昨天 18:20 / 周X 14:30 / 08-01 14:30 / 跨年 2025-12-28）
- F04 侧边栏宽屏折叠/展开：侧栏顶部收起按钮 + 折叠后展开条，localStorage 记住状态
- F05 删除/永久删除/删图片改自定义 ConfirmDialog，替换全部浏览器原生 confirm

---

## 变更前准备

### 备份（M4 §11 强制）

已备份至：`/home/ubuntu/backups/sticky-notes-20260805235424`

| 内容 | 大小 |
|------|------|
| `.env` | ✔ |
| `data/`（prod.db） | ✔ |
| `uploads/` | ✔ |
| Caddyfile | ✔ |
| 合计 | 6.8 MB |

### rsync 预演（dry-run）

- 同步项：src 下 8 个修改文件 + 4 个新增文件（AccountMenu / ConfirmDialog / Sidebar / format-time）+ 删除 TopBar
- 服务器哈希已与本地逐一比对（NoteEditor 558ee5…、page c9766a…、Sidebar 0dfe45…、format-time 062010…），完全一致
- 确认 `.env`、`data/`、`uploads/`、`*.db`、`.next/`、`node_modules/`、`src-tauri/` 等均未涉及

---

## 构建与重启

- 服务器 `npm run build` 成功（15 条路由生成，含 `/archive`、`/trash`、`/notes/[id]/floating`）
- 服务器已删除残留 `src/components/TopBar.tsx`
- `systemctl --user restart sticky-notes.service` → **active**（PID 332408）
- package.json 未变更，未执行 npm ci

---

## 健康检查结果

| 检查项 | 结果 |
|--------|------|
| 本地 curl 127.0.0.1:9002 根路径 | 307 → /login ✔ |
| 本地 curl /api/notes（未登录） | 401 ✔ |
| 登录接口（错误凭据） | 401 ✔ |
| 登录接口（正确凭据） | 200 ✔ |
| 登录后 /api/notes（cookie 会话） | 200，返回真实生产数据 ✔ |

---

## Windows 安装包

- CI：GitHub Actions run 31022617590（Build Windows Installer）**success**，构建产物 `windows-installer` artifact（5.5 MB）已生成
- Release v0.1.0 未变化（本轮纯前端改动，桌面壳加载 https://notes.hello-mylife.online，Web 端已更新到生产）：
  - 已装 v0.1.0 的用户无需重装，App 重新加载即取得新前端
  - 若需全新安装包，可下载本 run 的 `windows-installer` artifact 或继续使用 Release v0.1.0 安装包
- Release 页：https://github.com/zhewu-ai/win-/releases/tag/v0.1.0

---

## 服务与日志

systemd 状态：**active (running)**（2026-08-05 23:55:20 重启）

---

## 回滚方案

```bash
export XDG_RUNTIME_DIR="/run/user/$(id -u)"
systemctl --user stop sticky-notes.service
# 恢复备份 /home/ubuntu/backups/sticky-notes-20260805235424 中的 .env、data/、uploads/、Caddyfile
# 代码回滚：需恢复旧版本 src/ 源码并重新构建
npm run build
systemctl --user start sticky-notes.service
```

注意：本次无 schema 变更，回滚不涉及数据库结构。

---

## 剩余风险

1. **无 Git 历史**：服务器上代码仍无 `.git` 目录，更新依赖 rsync。建议后续在服务器初始化 Git 仓库。
2. **视觉项需 Windows 真机最终确认**：F01-F05 已在 260/320/1280px 浏览器视口以 DOM 几何测量验证（工具栏单行、侧栏底部入口、具体日期、折叠/展开、自定义弹窗），Windows 主窗同宽度下建议按验收指引再走一遍。
