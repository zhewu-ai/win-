# 交付报告 M14：工作日历开放全部用户

日期：2026-08-10
前置：`docs/发布前检查报告_20260810.md`（结论：可推送全部用户，无阻塞项）

## 一、结论

工作日历（M12 基础 + M13 AI 导入/调整）已从管理员专属开放为**所有已登录用户可用**，前端入口、API 权限、独立页均已放行。生产已部署并完成普通用户 / 管理员双端验收，无回归。

## 二、改动清单（commit f15d614）

| 分类 | 文件 | 改动 |
|---|---|---|
| API（11 处） | `work-projects`、`work-projects/[id]`、`work-schedule-items`、`work-schedule-items/[id]`、`work-schedule-items/quick-week`、`calendar-events`、`calendar-events/[id]`、`calendar-imports/parse`、`calendar-imports/confirm`、`calendar-adjustments/preview`、`calendar-adjustments/apply` | `requireAdmin()` → `requireUser()`，`userId` 仍按当前登录用户隔离（`admin.id` → `user.id`），**未改动**数据隔离逻辑 |
| 独立页 | `app/(protected)/calendar/page.tsx` | 移除 `requireAdmin` 守卫与 `calendar_forbidden` 重定向（`(protected)` 布局已保证登录态），保留深链 `?date/new/event` |
| 首页 | `app/(protected)/page.tsx` | 移除 `isAdmin &&` 日历门槛（`showCalendarPanel`、日历层渲染）、`isAdmin` prop、`useCurrentUser` 依赖、`calendar_forbidden` 死代码 |
| 组件 | `Sidebar.tsx` / `NoteList.tsx` | 移除 `isAdmin` prop 链；`showCalendarGroup` 改为 `Boolean(onOpenCalendar)`，日历入口对所有用户可见 |

**未改动**：数据模型、权限隔离逻辑（`isProjectOwnedBy` / `isNoteOwnedBy` 按用户隔离）、管理后台（`/api/admin/*`、`/admin/users`、AccountMenu 管理入口按 `role === "admin"`）、AI 配额/频率策略（保持完全开放）。

## 三、验证

### 3.1 dev 端（普通用户 m12_403_test@example.com）

- 侧边栏列表顶部出现「日历」分组 + 工作日历入口卡片（今日预览正常）✓
- 点击入口 → URL 保持 `/`，右侧打开工作日历（周视图 + 「暂无项目」+「新增项目」）✓
- 工作日历 API 全部 200（work-projects / calendar-events / work-schedule-items / imports / adjustments）✓
- 管理后台仍 403/跳首页（api/admin/invites 403、/admin/users → 307 回首页）✓
- 截图：`/Users/dian/Desktop/开放全部用户-普通用户日历入口.png`

### 3.2 生产端 `https://notes.hello-mylife.online`

健康检查：/login 200、未登录 / 307、/api/auth/me 401、admin 登录 200 ✓

普通用户（邀请码注册临时账号，验证后已删除，生产恢复 1 admin + 7 用户）：

| 端点 | 结果 |
|---|---|
| `work-projects` GET | 200 |
| `calendar-events?from..to` GET | 200 |
| `work-schedule-items` GET | 200 |
| `calendar-adjustments/preview` POST（非法体） | 400（过权限、校验拦截） |
| `calendar-imports/parse` POST（无文件） | 400（过权限） |
| `/calendar` 独立页 | 200 |
| `api/admin/invites` | 403（管理后台未放开） |
| `/admin/users` | 307 → 首页 |

管理员：work-projects / calendar-events / admin/invites / /admin/users / /calendar 全部 200 ✓

## 四、风险与后续建议

- **无阻塞项、无回归**。数据隔离按 userId 保持，普通用户各管各的项目/排期。
- AI 成本：保持「完全开放」决策，建议运营侧关注 `usage.durationMs` / token 用量日志（`docs/发布前检查报告_20260810.md` §四.6）。
- 空状态引导：普通用户首次进入工作日历无项目，当前显示「暂无项目」+「新增项目」按钮；后续可补首次使用引导（非阻塞）。
- P2 体验项：连续月历改按周渲染，列为后续迭代。
- 建议开放后安排一轮「普通用户真实使用」观察（建项目、AI 导入/调整首用反馈）。

## 五、部署记录

- DB 备份：`/home/ubuntu/backups/prod_M14_open_20260810_142806.db`
- rsync `src/`（--delete）→ 服务器 `rm -rf .next && npm run build -- --webpack` → 重启 sticky-notes.service（active）
- 验证后删除临时验收用户 `m14_open_1786343362@example.com`（含其 4 条示例便签）
