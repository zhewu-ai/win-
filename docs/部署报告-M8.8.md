# M8.8 部署报告：离线可用 MVP 与本地自动保存

> 提交日期：2026-08-06
> 提交：工作区未提交（本轮改动在本地工作树，经 rsync 直接部署）
> 部署类型：增量更新（M8.8 离线 MVP，对应 `SPEC_M8_8_OFFLINE_MVP_20260806.md`）到已在线的生产环境

---

## 部署概况

项目路径：`/home/ubuntu/projects/sticky-notes`

更新方式：rsync 增量同步（`src/` 带 `--delete` + 新增 `public/` + 根配置）

服务：`sticky-notes.service`（systemd --user），监听 `127.0.0.1:9002`，域名 `https://notes.hello-mylife.online`

### 本次新增/修改文件

| 文件 | 类型 |
|---|---|
| `src/lib/offline/db.ts` | 新增（Dexie 数据层） |
| `src/lib/offline/persist.ts` | 新增（Note↔LocalNote 映射 + 离线写路径） |
| `src/lib/offline/sync.ts` | 新增（同步引擎） |
| `src/hooks/useNotes.ts` | 修改（离线感知路由 + 在线镜像 + 已删便签 reconcile） |
| `src/hooks/useSyncStatus.ts` | 新增（在线/同步状态 + pending 计数） |
| `src/components/OfflineBar.tsx` | 新增（离线状态条） |
| `src/components/SaveStatus.tsx` | 修改（3 态 → 5 态文案） |
| `src/components/ImageUploadButton.tsx` | 修改（离线禁用 + tooltip） |
| `src/components/AccountMenu.tsx` | 修改（有 pending 时登出确认） |
| `src/components/SWRegister.tsx` | 新增（生产环境注册 Service Worker） |
| `src/app/layout.tsx` | 修改（挂 SWRegister，title → PinNote） |
| `src/app/login/LoginForm.tsx` | 修改（离线无法登录提示） |
| `src/app/notes/[id]/floating/FloatingNoteClient.tsx` | 修改（离线浮窗缓存回填） |
| `src/app/(protected)/page.tsx` | 修改（离线列表回填接线） |
| `src/components/NoteEditor.tsx` | 修改（离线保存路径接线） |
| `src/types/index.ts` | 修改（SyncStatus 类型） |
| `public/sw.js` | 新增（离线 App Shell） |
| `next.config.mjs` | 修改（/sw.js 响应头） |
| `package.json` / `package-lock.json` | 修改（新增 dexie ^4.4.4） |

Tauri 侧（不进本服务器，随 GitHub Actions 出 Windows 安装包）：`shell-static/index.html`（bootstrapper + 离线 fallback）、`src-tauri/src/lib.rs`（窗口加载 bootstrap 页 + 悬浮窗 hash 路由）。

无 schema 变更，无需迁移。

---

## 功能内容（对应 SPEC M8.8）

### 1. 离线可用 MVP（核心）

- **断网打开 App**：Service Worker 缓存 App Shell（network-first，失败回退缓存 "/"）；Tauri 生产包经本地 bootstrap 页探测连通性 → 在线跳线上域名 / 离线显示 fallback。
- **离线浏览**：fetchNotes 失败时从 Dexie（IndexedDB）回填列表，排除已删除。
- **离线新建**（普通 + 待办）：本地 UUID 为 id，`pendingCreate`，写 IndexedDB + 插入列表。
- **离线编辑**：`pendingUpdate`，自动保存到本地。
- **离线删除**：未同步便签直接移除；已同步便签标 `pendingDelete` 并从列表隐藏。
- **关闭重开不丢**：Dexie 持久化，重开后从缓存回填。

### 2. 本地自动保存（SaveStatus 5 态）

`已同步` / `已保存到本地`（离线+pending）/ `等待同步`（在线+pending）/ `同步中` / `同步失败`。离线输入后显示「已保存到本地」，绝不在离线显示「已同步」。

### 3. 同步引擎（联网自动推送）

- 顺序：`pendingCreate` → `syncError` → `pendingUpdate` → `pendingDelete`。
- pendingCreate：POST 建单（title/content/color/mode/isPinned）→ 再 PATCH 全量（isArchived/checklistGroups/sortOrder）→ 回填 serverId → synced。
- pendingUpdate：PATCH `/api/notes/:serverId` 全字段。
- pendingDelete：DELETE → 本地移除。
- 触发：online 事件、App 挂载（在线）、手动重试、30s 定时轻量重试。
- 冲突策略（本机最后编辑优先）：在线成功后 `mirrorNote` 遇到本地非 `synced` 行跳过写入，避免服务端快照吞掉离线编辑。

### 4. 图片离线降级

离线时 ImageUploadButton 禁用 + tooltip「离线模式下暂不支持上传图片」。

### 5. 离线认证降级

登录页离线时提示「离线状态无法登录，联网后重试」；已登录会话依赖 SSR 会话 cookie + SW 缓存的 App Shell，断网后仍可进入缓存会话浏览/编辑（服务器渲染在在线时已完成，离线走缓存页面 + 本地数据）。

---

## 验证记录

### 本地构建

- `npx tsc --noEmit`：通过，无类型错误。
- `npm run build`：通过，11 个页面全部生成（含新增 `/notes/[id]/floating` 等）。

### 离线模拟验证（dev 环境，eval 覆盖 fetch + onLine + offline 事件）

| 场景 | 结果 |
|---|---|
| 离线启动：列表从 Dexie 回填，OfflineBar 显示离线 | 通过 |
| 离线新建普通便签 → 输入 → reload → 仍在（pendingCreate） | 通过 |
| 离线新建待办 → 3 条 + 勾选 1 条 → reload → 待办与勾选状态仍在 | 通过 |
| 离线编辑已缓存便签 → reload → 修改仍在（pendingUpdate） | 通过 |
| 离线删除已缓存便签 → 列表隐藏（pendingDelete） | 通过 |
| 离线图片上传禁用 + tooltip；SaveStatus 显示「已保存到本地」不显示「已同步」 | 通过 |
| 联网 runSync：pendingCreate 上线可见、pendingUpdate/PendingDelete 与服务器一致 | 通过 |
| 镜像保护回归：离线编辑(pendingUpdate) 联网后经 fetchNotes 镜像路径不被服务端快照吞掉，随后 runSync 推送到服务器 | 通过 |

### 生产部署

1. 备份：`data/prod.db` → `/home/ubuntu/backups/prod_M8_8_20260806-153922.db`
2. rsync：`src/`（--delete）+ `public/`（新增）+ `next.config.mjs` + `package.json` + `package-lock.json`
3. 服务器 `npm install`（拉取 dexie）+ `npm run build`：通过
4. `systemctl --user restart sticky-notes.service`：active

### 健康检查（部署后）

| 端点 | 期望 | 实际 |
|---|---|---|
| `/login` | 200 | 200 |
| `/`（未登录） | 307 → /login | 307 |
| `/api/auth/me`（未登录） | 401 | 401 |
| `/sw.js` | 200 + `Content-Type: application/javascript` + `Cache-Control: no-cache` | 全部符合 |

---

## 未验证项（转交实机/浏览器验收）

- **生产 SW 运行时**：断网刷新从 SW 缓存出 App。本环境 Chrome 扩展未连接、preview 浏览器锁 localhost origin，无法在线上域名实测 DevTools offline → 转浏览器/Windows 实机验收项。
- **Windows 实机（本环境无法运行）**：断网冷启动不显示错误页（Tauri fallback）、离线新建/编辑关闭重开不丢、联网自动同步录屏 → 见 `M8.8_WINDOWS_ACCEPTANCE_GUIDE_20260806.md`。

---

## 风险与后续

- **SW 缓存冷启动取舍**：Tauri 离线冷启动且无 SW 缓存时显示本地 fallback 页（探测 + 重试），而非缓存 App；若要严格「离线冷启动也显示缓存便签」，需远程优先导航 + Rust 导航失败拦截（Tauri v2 需验证事件暴露）。
- SW 首次需在线安装一次；从未在线的 Tauri 冷启动由 fallback 兜底（仅提示 + 重试）。
- Dexie 数据在浏览器/WebView 用户目录，跨设备不共享（符合单人本地缓存定位）。
- 多端冲突的复杂合并留后续版本；本轮「本机最后编辑优先」覆盖服务器改动。
