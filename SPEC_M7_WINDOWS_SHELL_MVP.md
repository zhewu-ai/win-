# 全平台便签 M7：Windows 桌面壳 MVP

日期：2026-08-02

## 1. 背景

当前 Web 版便签已经大致可用，核心问题变成：网页无法像 Windows 便签一样稳定悬浮、置顶在桌面上。

本轮 M7 不重新做一套桌面端业务逻辑，而是做一个 Windows WebView 壳，让桌面 App 加载线上 Web 应用。后续 Web 端更新后，Windows App 自动获得新功能。

## 2. 本轮目标

M7 只验证一个核心价值：

> 用户可以把某条便签/待办以小窗口形式置顶在 Windows 桌面上，用来处理工作反馈。

必须做到：

1. Windows 上可安装/运行。
2. App 内加载线上 Web 版便签。
3. 支持窗口置顶。
4. 支持小窗口使用。
5. 支持保存登录态。
6. 支持恢复上次窗口大小和位置。
7. 支持打开单条便签悬浮视图。

## 3. 本轮不做

1. 不做完整 Windows 原生便签重写。
2. 不做离线编辑。
3. 不做本地数据库同步。
4. 不做邮箱注册/邮箱登录。
5. 不做多用户权限系统。
6. 不做 Mac 端壳。
7. 不做手机原生 App。
8. 不做应用商店发布。

账号体系放到 M8 单独做。本轮只保证当前登录态能在 Windows 壳内保存和复用。

## 4. 推荐技术方案

优先推荐 Tauri。

原因：

1. 安装包比 Electron 小。
2. 适合“WebView 套壳 + 系统窗口能力”。
3. 支持窗口置顶、窗口尺寸、窗口位置、托盘等桌面能力。
4. 当前业务逻辑仍然留在 Web 端，桌面壳只负责桌面体验。

备选方案：

- Electron：能力完整，但包体更大，当前阶段不优先。

## 5. App 加载地址

生产环境默认加载：

```text
https://notes.hello-mylife.online
```

开发环境允许配置：

```text
http://localhost:3000
```

要求：

1. URL 必须通过配置读取，不要硬编码散落在代码里。
2. 至少支持 `production` 和 `development` 两套配置。
3. 生产包默认不能误连本地开发地址。

## 6. 窗口形态

### 6.1 主窗口

用途：

- 打开完整 Web 应用。
- 管理便签列表、搜索、回收站、归档等。

建议默认尺寸：

```text
宽度：1100px
高度：760px
最小宽度：900px
最小高度：600px
```

要求：

1. 登录态可保存。
2. 关闭后再次打开，应恢复上次窗口大小和位置。
3. 主窗口可以不默认置顶。

### 6.2 悬浮便签窗口

用途：

- 只显示单条便签，用于桌面置顶。

加载地址：

```text
/notes/:id/floating
```

建议默认尺寸：

```text
宽度：360px
高度：520px
最小宽度：320px
最小高度：360px
```

要求：

1. 默认支持置顶。
2. 可以开关置顶。
3. 无侧边栏。
4. 小窗口下工具栏不能换行、竖排或遮挡。
5. 关闭后恢复上次窗口大小和位置。
6. 如果没有登录或登录失效，应跳转登录页，登录成功后回到该便签。

## 7. 置顶能力

必须提供：

1. `Always on Top` 开关。
2. 悬浮窗口默认开启置顶。
3. 主窗口可手动开启置顶，但默认关闭。
4. 置顶状态应保存，下次打开恢复。

验收：

1. 打开 C4D/AE 等其他软件后，悬浮便签仍停留在前方。
2. 关闭置顶后，窗口可被其他软件遮挡。
3. 重新打开 App 后，置顶状态按上次设置恢复。

## 8. 登录态与账号边界

### 8.1 M7 登录态要求

本轮不新增邮箱注册/登录系统。

Windows 壳只需要：

1. 能打开现有 Web 登录页。
2. 登录后 Cookie/session 在 App 内保存。
3. 关闭 App 再打开，不应频繁要求重新登录。
4. 退出登录后，壳内状态清理干净。

### 8.2 M8 再做账号体系

当产品准备给同事使用时，再做 M8：

1. 邮箱注册。
2. 邮箱登录。
3. 找回密码或验证码登录。
4. 用户表。
5. 每条便签绑定 `userId`。
6. API 服务端强校验用户权限。
7. 邀请机制或白名单机制。

不要在 M7 临时拼一个不完整账号系统，否则会污染后续权限设计。

## 9. Web 端需要配合的接口/页面

### 9.1 单条便签悬浮页

页面：

```text
/notes/:id/floating
```

当前如已存在，需要复验小窗口体验；如未完善，需要补齐。

要求：

1. 只显示单条便签。
2. 不显示列表侧边栏。
3. 保留编辑、待办切换、颜色、图片、置顶、删除。
4. 320px 宽度下可用。
5. 不依赖鼠标 hover 才能发现关键操作。

### 9.2 桌面壳状态字段

如果后端已有以下字段，本轮需要验证可读写：

```text
windowX
windowY
windowWidth
windowHeight
alwaysOnTop
```

如果这些字段尚未完整接入，M7 可先存在本地配置中；但要给后端字段预留迁移路径。

## 10. 安全要求

1. 桌面壳不要绕过 Web 登录校验。
2. 不要把账号、密码、token 明文写入配置文件。
3. 不要在客户端硬编码服务端密钥。
4. 仅允许加载可信域名：

```text
https://notes.hello-mylife.online
http://localhost:3000
```

5. 生产包默认禁用任意外部 URL 跳转，或使用系统浏览器打开外部链接。

## 11. 安装包要求

MVP 阶段至少提供：

1. Windows 可执行安装包。
2. Windows 免安装包可选。
3. README 写明：
   - 如何安装
   - 如何启动
   - 如何切换开发/生产地址
   - 如何打开悬浮窗口
   - 如何开启/关闭置顶

暂不要求代码签名。

## 12. 验收清单

技术部交付后，必须提供以下证据：

1. Windows 上启动 App 成功。
2. 主窗口加载 `https://notes.hello-mylife.online` 成功。
3. 登录后关闭 App，再打开仍保持登录态。
4. 可以从主窗口打开某条便签的悬浮窗口。
5. 悬浮窗口加载 `/notes/:id/floating`。
6. 悬浮窗口宽度 320px 时仍可编辑。
7. 悬浮窗口开启置顶后，切到其他 Windows 软件仍保持在前。
8. 关闭置顶后，可以被其他窗口遮挡。
9. 调整悬浮窗口大小和位置，关闭重开后恢复。
10. 主窗口和悬浮窗口不会互相抢焦点到不可用。
11. 退出登录后，再打开 App 不应直接进入已登录页面。
12. 生产包默认加载生产域名，不加载 localhost。
13. 构建 Windows 安装包通过。

## 13. 成功标准

M7 通过的标准不是“桌面 App 功能完整”，而是：

> 用户能在 Windows 上把一条便签置顶悬浮，用它处理真实工作反馈，并且 Web 端后续更新能自然同步到这个壳里。

只要这个目标跑通，就可以进入 M8 账号体系，而不是在 M7 继续扩大范围。
