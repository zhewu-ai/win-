# 全平台便签 Web MVP 产品规格

## 1. 范围

本规格定义第一版 Web MVP。第一版只实现网页端和服务端能力，不实现任何原生桌面端或移动端。

未来桌面端预留原则：

- Web 页面可以被 Windows/Mac 壳加载。
- API 不依赖浏览器专有上下文。
- 认证状态可以被桌面壳持久化。
- Note 模型保留窗口相关字段。

## 2. 用户和认证

### 2.1 单用户模式

第一版采用单用户私有模式。

要求：

- 系统初始化时支持创建一个管理员用户。
- 登录页只需要用户名和密码。
- 不开放公开注册。
- 不需要找回密码。

### 2.2 登录

输入：

- `username`
- `password`

成功：

- 返回登录成功状态。
- 客户端进入便签主页。
- 后续 API 请求带认证状态。

失败：

- 用户名或密码错误时显示错误提示。
- 错误提示不能暴露用户是否存在。

验收：

- 未登录访问主页时跳转到登录页。
- 登录成功后刷新页面仍保持登录态。
- 退出登录后再次访问主页需要重新登录。

## 3. 便签模型

### 3.1 字段

`Note` 字段：

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | string | 是 | 自动生成 | 便签 ID |
| `userId` | string | 是 | 当前用户 | 所属用户 |
| `title` | string | 否 | 空字符串 | 标题 |
| `content` | string | 否 | 空字符串 | 正文 |
| `color` | string | 是 | `yellow` | 颜色 |
| `isPinned` | boolean | 是 | `false` | 是否置顶 |
| `isArchived` | boolean | 是 | `false` | 是否归档 |
| `sortOrder` | number | 是 | 当前时间戳 | 排序 |
| `windowX` | number/null | 否 | null | 预留桌面窗口 X |
| `windowY` | number/null | 否 | null | 预留桌面窗口 Y |
| `windowWidth` | number/null | 否 | null | 预留桌面窗口宽度 |
| `windowHeight` | number/null | 否 | null | 预留桌面窗口高度 |
| `alwaysOnTop` | boolean | 是 | `false` | 预留桌面置顶 |
| `createdAt` | string | 是 | 自动生成 | 创建时间 |
| `updatedAt` | string | 是 | 自动更新 | 更新时间 |
| `deletedAt` | string/null | 否 | null | 软删除时间 |

### 3.2 颜色枚举

第一版颜色：

- `yellow`
- `blue`
- `green`
- `pink`
- `gray`

前端显示颜色名称或色块，不允许用户输入任意颜色字符串。

## 4. API 规格

API 路径可以按技术栈调整，但行为必须满足本节。

### 4.1 登录

`POST /api/auth/login`

请求：

```json
{
  "username": "admin",
  "password": "password"
}
```

成功响应：

```json
{
  "ok": true,
  "user": {
    "id": "user_id",
    "username": "admin"
  }
}
```

失败响应：

```json
{
  "ok": false,
  "error": "INVALID_CREDENTIALS"
}
```

### 4.2 退出登录

`POST /api/auth/logout`

成功响应：

```json
{
  "ok": true
}
```

### 4.3 获取当前用户

`GET /api/auth/me`

未登录返回 401。

### 4.4 获取便签列表

`GET /api/notes?archived=false&q=keyword`

查询参数：

- `archived`：`true` 或 `false`，默认 `false`
- `q`：搜索关键词，可选

返回：

```json
{
  "notes": []
}
```

排序规则：

1. `isPinned=true` 在前。
2. 同一置顶状态内，`updatedAt` 倒序。
3. 不返回 `deletedAt` 非空的便签。

搜索规则：

- `q` 为空时返回全部未删除便签。
- `q` 非空时匹配 `title` 和 `content`。
- 第一版可以使用不区分大小写的包含匹配。

### 4.5 新建便签

`POST /api/notes`

请求：

```json
{
  "title": "",
  "content": "",
  "color": "yellow"
}
```

返回创建后的完整 `Note`。

规则：

- `title`、`content` 可为空。
- `color` 不传时使用 `yellow`。
- 新便签默认 `isPinned=false`、`isArchived=false`。

### 4.6 更新便签

`PATCH /api/notes/:id`

允许更新字段：

- `title`
- `content`
- `color`
- `isPinned`
- `isArchived`
- `sortOrder`
- `windowX`
- `windowY`
- `windowWidth`
- `windowHeight`
- `alwaysOnTop`

规则：

- 更新成功后刷新 `updatedAt`。
- 只能更新当前用户自己的便签。
- 更新不存在、已删除或不属于当前用户的便签时返回 404。
- 非法颜色返回 400。

### 4.7 删除便签

`DELETE /api/notes/:id`

规则：

- 执行软删除，设置 `deletedAt`。
- 删除后列表接口不再返回该便签。
- 重复删除同一便签可以返回 404 或幂等成功，由技术部统一实现并在接口文档中固定。

## 5. 页面规格

### 5.1 登录页

组件：

- 用户名输入框
- 密码输入框
- 登录按钮
- 错误提示区域

行为：

- 点击登录后调用登录 API。
- 登录成功进入主页。
- 登录失败显示错误。
- 登录请求中按钮需要有 loading 状态。

### 5.2 主页

组件：

- 顶部栏
- 搜索框
- 新建按钮
- 便签列表
- 编辑区域
- 退出登录入口

空状态：

- 没有便签时显示空状态。
- 搜索无结果时显示搜索空状态。

列表显示：

- 标题为空时展示正文前若干字作为摘要。
- 标题和正文都为空时展示“无标题便签”。
- 显示更新时间。
- 显示颜色。
- 置顶便签有明确标识。

### 5.3 编辑区域

组件：

- 标题输入
- 正文输入
- 颜色选择
- 置顶开关
- 归档按钮
- 删除按钮
- 保存状态

行为：

- 用户修改标题、正文、颜色、置顶状态后自动保存。
- 自动保存建议 debounce 500-1000ms。
- 保存期间显示“保存中”。
- 保存成功显示“已保存”。
- 保存失败显示“保存失败”，并允许继续编辑。
- 删除前需要确认。

### 5.4 归档页

组件：

- 归档便签列表
- 搜索框
- 返回主页入口
- 取消归档操作
- 删除操作

规则：

- 归档便签不显示在主页。
- 归档页只显示 `isArchived=true` 且未删除的便签。

## 6. 响应式规格

桌面宽屏：

- 左侧便签列表，右侧编辑区域。

窄屏或手机浏览器：

- 默认显示便签列表。
- 点击便签后进入编辑视图。
- 编辑视图提供返回列表入口。
- 顶部操作不能遮挡编辑内容。

未来桌面壳小窗口：

- 宽度 320px 时仍可编辑标题和正文。
- 操作按钮允许折叠到菜单中。
- 正文区域优先保留可输入空间。

## 7. 自动保存和同步

### 7.1 自动保存

触发：

- 标题变化
- 正文变化
- 颜色变化
- 置顶变化
- 归档变化

规则：

- 输入类字段使用 debounce。
- 开关类字段可以立即保存。
- 保存失败时保留本地输入，不回滚到旧服务端值。
- 用户离开当前便签前，如有未保存内容，应尽量触发一次保存。

### 7.2 同步刷新

触发：

- 页面首次加载。
- 浏览器 tab 从后台回到前台。
- 用户手动刷新。
- 用户清空或修改搜索条件。

冲突规则：

- 第一版采用最后写入生效。
- 服务端以最后一次成功 PATCH 的内容为准。
- 前端不做复杂冲突合并。

## 8. 错误和边界状态

必须处理：

- 未登录
- 登录失败
- 列表加载失败
- 新建失败
- 保存失败
- 删除失败
- 空列表
- 搜索无结果
- 网络中断
- 服务端 500

保存失败时：

- 当前输入内容必须留在界面。
- 提示用户保存失败。
- 用户继续输入后可以再次触发保存。

## 9. 安全要求

最低要求：

- 密码不能明文存储。
- 服务端接口必须校验登录态。
- 用户只能访问自己的便签。
- 生产环境必须使用 HTTPS。
- Cookie 或 token 需要设置合理过期策略。

建议：

- 使用 HTTP-only Cookie 保存 session。
- 登录失败做简单频率限制。
- 服务端日志不记录明文密码。

## 10. 部署要求

生产环境需要：

- 域名访问
- HTTPS
- 数据库持久化
- 服务重启后数据不丢失
- 基础错误日志
- 可配置管理员账号初始密码

环境变量建议：

- `DATABASE_URL`
- `SESSION_SECRET`
- `ADMIN_USERNAME`
- `ADMIN_PASSWORD`
- `APP_BASE_URL`

## 11. 测试清单

技术部交付前至少完成以下验证：

1. 未登录不能访问主页。
2. 正确账号可以登录。
3. 错误账号不能登录。
4. 新建空便签成功。
5. 编辑标题后自动保存。
6. 编辑正文后自动保存。
7. 刷新页面后内容存在。
8. 置顶后排序到普通便签前。
9. 归档后主页不显示。
10. 归档页显示归档便签。
11. 取消归档后主页重新显示。
12. 删除后主页和归档页都不显示。
13. 搜索可以匹配标题。
14. 搜索可以匹配正文。
15. 搜索无结果时显示空状态。
16. 手机浏览器可以新建、编辑、搜索。
17. 模拟网络失败时保存状态显示失败，输入内容不丢失。
18. 服务重启后数据仍存在。

## 12. 完成定义

本 MVP 视为完成，当且仅当：

- `MVP.md` 中的必须交付项全部实现。
- 本规格的 API 行为和页面行为全部满足。
- 测试清单全部通过。
- 项目可以部署到服务器并通过正式域名访问。
- 技术部给出部署步骤、环境变量说明和管理员账号初始化方式。

