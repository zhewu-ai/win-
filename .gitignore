node_modules/
.next/
*.db
*.db-journal
.env
.env.local
tsconfig.tsbuildinfo

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
# 本地 cargo 镜像加速配置（不影响 CI / 服务器）
.cargo/

# 运行时数据（用户上传图片，不入库）
uploads/
# 运行时数据库与本地邮件落盘（不入库）
data/

# macOS
.DS_Store

# Claude Code 本地配置（含本地绝对路径，不入库）
.claude/
