# 全平台便签 M8.8：离线可用 MVP 与本地自动保存

日期：2026-08-06

## 0. 产品命名

从 M8.8 开始，产品对外显示名暂定为：

```text
PinNote
```

中文描述名仍可使用：

```text
全平台便笺
```

命名说明：

- `PinNote` 对应“置顶、钉在桌面上的便签”，贴合 Windows 桌面壳和悬浮便签定位。
- 名字短、好读，不把产品限制成纯清单或纯待办。
- 本轮只要求 UI 显示名、文档和后续验收口径统一为 `PinNote`。

本轮不强制修改：

1. 生产域名：`notes.hello-mylife.online` 暂不调整。
2. 服务器目录、systemd 服务名、Caddy 配置中的内部命名暂不调整。
3. 数据库表名、API 路径、代码包名暂不调整。

原因：M8.8 的核心目标是离线可用和本地自动保存，不应为了改名引入部署、同步或数据迁移风险。后续如果产品名稳定，再单独安排一次低风险命名清理。

## 1. 背景

Windows 桌面壳目前本质上加载线上网页：

```text
https://notes.hello-mylife.online
```

当电脑暂时没有网络时，App 会显示 WebView/浏览器错误页。这会明显暴露“网页套壳”的感觉，也会影响便签产品最基础的使用场景：

> 用户临时断网时，仍然希望打开便签、记录内容、稍后联网自动同步。

产品侧判断：只做离线只读缓存不够。便签工具的基础价值是“随手记”，因此 M8.8 应支持离线新建和编辑文字/待办，并本地自动保存。

但本轮不做完整复杂 offline-first 系统，不处理复杂多人冲突，不做图片离线上传。

## 2. 本轮目标

M8.8 目标：

1. 断网时 Windows App 不显示浏览器错误页。
2. 断网时可以打开 App。
3. 断网时可以查看最近已缓存便签。
4. 断网时可以新建普通便签。
5. 断网时可以新建待办便签。
6. 断网时可以编辑已缓存便签。
7. 断网编辑内容必须自动保存到本地。
8. 关闭 App 再打开，离线写入内容仍然存在。
9. 联网后自动同步到服务器。
10. 同步状态要明确显示，用户知道哪些内容还没同步。

### 2.1 最低可交付定义

本轮最低可交付不是“断网时有一个提示页”，而是：

```text
断网可打开 -> 断网可新建/编辑 -> 自动保存到本地 -> 关闭重开不丢 -> 联网后同步
```

如果只能离线查看，不能离线新建/编辑，不算通过。

如果能离线编辑，但刷新、关闭 App 或重启电脑后内容丢失，也不算通过。

## 3. 本轮不做

1. 不做图片离线上传。
2. 不做图片离线缓存完整方案。
3. 不做复杂冲突合并 UI。
4. 不做多人协作。
5. 不做离线账号登录。
6. 不做端到端加密。
7. 不做本地文件夹 Markdown 存储。
8. 不做完整 PWA 安装体验。

## 4. 产品原则

### 4.1 离线可写必须本地持久化

如果允许离线新建/编辑，就必须保证：

```text
断网输入 -> 自动保存到本地 -> 关闭 App -> 再打开仍在
```

不能出现：

```text
断网能写，但关闭后丢失
```

这会比只读离线更糟糕。

这里的“关闭后”包含：

1. 关闭当前窗口。
2. 完全退出 Windows App 后重新打开。
3. 断网状态下刷新 WebView 或重新加载页面。
4. 电脑睡眠/唤醒后重新进入 App。

### 4.2 第一版同步规则简单优先

当前主要是个人使用，多端冲突概率低。MVP 不需要一开始做复杂合并。

推荐第一版：

```text
本机最后编辑优先
```

也就是：

- 离线期间本机修改的便签，联网后推送到服务器。
- 如果服务器同一条便签也被改过，MVP 可先按本机版本覆盖。
- 需要记录日志或状态，后续再扩展冲突提示。

### 4.3 图片先降级

图片离线复杂度高，涉及：

- 本地文件存储。
- 图片压缩。
- 上传队列。
- 上传失败重试。
- 附件和便签的关联同步。

本轮先不做。

离线时：

- 图片上传按钮禁用；或
- 点击提示：

```text
离线模式下暂不支持上传图片，联网后可上传。
```

在线已有图片：

- 如果浏览器缓存中已有缩略图，可以展示。
- 如果没有缓存，显示占位，不阻塞文字/待办使用。

## 5. 推荐技术方案

### 5.1 本地存储

优先推荐：

```text
IndexedDB
```

原因：

1. Web 端和 Windows 壳都能使用。
2. 适合结构化数据和离线队列。
3. 不需要 Tauri 单独做 SQLite 绑定。
4. 后续如果做 PWA，也能复用。

可选库：

- 原生 IndexedDB。
- idb。
- Dexie。

如果技术部认为原生 IndexedDB 太繁琐，建议使用 Dexie 或 idb，避免手写大量样板代码。

### 5.2 不推荐第一版使用 Tauri SQLite

Tauri SQLite 也可行，但本轮不优先。

原因：

1. 会把 Windows 壳和 Web 端逻辑拆开。
2. 后续 Web/PWA 无法复用。
3. 增加桌面端特定维护成本。

M8.8 目标是先实现高性价比离线能力，因此优先 Web 层 IndexedDB。

### 5.3 App Shell 缓存

必须保证断网时 App 能打开基本 UI。

建议：

1. 增加 Service Worker 或等价缓存策略。
2. 缓存 App shell 必要资源：
   - HTML shell
   - JS/CSS bundle
   - 基础图标/字体，如有
3. 网络失败时不要显示 WebView 默认错误页，而是显示产品内离线界面。

Windows 壳补充：

- 如果远程页面完全无法加载，Tauri 侧需要一个本地 fallback 页面。
- fallback 页面至少显示：

```text
当前离线
正在使用本地缓存
重试连接
```

如果 Web Service Worker 已能覆盖该场景，可以不做 Tauri 本地 fallback；但必须用 Windows 断网实测证明不会出现浏览器错误页。

## 6. 数据模型建议

### 6.1 本地便签表

IndexedDB 建议表：

```ts
LocalNote {
  id: string;              // 服务器 id；离线新建前可为空或使用 local id
  localId: string;         // 本地稳定 id
  title: string;
  content: string;
  color: string;
  isPinned: boolean;
  isArchived: boolean;
  mode: "text" | "checklist";
  checklistItems: unknown[];
  checklistGroups: unknown[];
  attachments?: unknown[]; // 在线缓存可读，离线不新增
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  syncStatus: "synced" | "pendingCreate" | "pendingUpdate" | "pendingDelete" | "syncError";
  lastSyncedAt: string | null;
  lastLocalEditAt: string;
}
```

### 6.2 同步队列表

也可单独维护：

```ts
SyncQueueItem {
  id: string;
  localNoteId: string;
  action: "create" | "update" | "delete";
  payload: unknown;
  createdAt: string;
  retryCount: number;
  lastError: string | null;
}
```

简单版可不单独建队列表，直接通过 `syncStatus` 扫描待同步便签。

推荐：

- M8.8 用 `syncStatus` 简化。
- 后续复杂同步再引入独立队列表。

## 7. 离线行为规格

### 7.1 启动

在线启动：

1. 正常加载服务器数据。
2. 成功获取便签列表后，写入本地缓存。
3. UI 显示在线/已同步状态。

离线启动：

1. 不显示浏览器错误页。
2. 从 IndexedDB 加载本地缓存。
3. 显示离线状态条：

```text
离线模式，修改将保存在本地，联网后自动同步
```

4. 如果没有任何本地缓存，显示：

```text
当前离线，暂无本地便签缓存
```

并允许新建本地便签。

要求：

- 离线首次打开时，如果本机没有缓存，也不能直接显示浏览器错误页。
- 离线空状态下的新建按钮必须可用。
- 离线空状态下创建的第一条便签必须进入本地持久化流程。

### 7.2 离线新建普通便签

行为：

1. 用户点击新建。
2. 创建本地便签。
3. `syncStatus = pendingCreate`。
4. 输入标题/正文时自动保存到 IndexedDB。
5. 关闭 App 再打开，便签仍存在。

验收：

1. 断网。
2. 新建普通便签。
3. 输入标题和正文。
4. 关闭 App。
5. 保持断网重新打开。
6. 新便签仍在，内容不丢。

### 7.3 离线新建待办

行为：

1. 用户新建便签后切到待办，或直接新建待办。
2. checklist 数据写入本地。
3. Enter 新增待办、本地排序、小标题等都保存到 IndexedDB。
4. `syncStatus = pendingCreate` 或 `pendingUpdate`。

验收：

1. 断网。
2. 新建待办便签。
3. 添加 3 条待办。
4. 勾选 1 条。
5. 关闭 App 后重开。
6. 待办状态仍在。

### 7.4 离线编辑已缓存便签

行为：

1. 用户打开已缓存便签。
2. 修改标题、正文、待办。
3. 自动保存到 IndexedDB。
4. 如果原状态是 `synced`，改为 `pendingUpdate`。
5. 如果原状态是 `pendingCreate`，保持 `pendingCreate`。

验收：

1. 在线打开一条便签，确认已缓存。
2. 断网。
3. 修改内容。
4. 关闭 App。
5. 离线重开，修改仍存在。
6. 联网后同步到服务器。

### 7.5 离线删除

MVP 建议支持。

行为：

- 删除已同步便签：本地标记 `pendingDelete`，列表中隐藏，联网后调用服务器删除。
- 删除离线新建但未同步便签：直接从本地删除，不需要同步服务器。

验收：

1. 离线删除已缓存便签。
2. 列表中立即消失。
3. 联网后服务器进入回收站。

## 8. 联网后同步

### 8.1 触发时机

同步触发：

1. `window` 从 offline 变 online。
2. App 启动时检测在线。
3. 用户点击“重试同步”。
4. 每隔一段时间后台轻量重试，例如 30 秒到 60 秒。

### 8.2 同步顺序

建议：

1. `pendingCreate`
2. `pendingUpdate`
3. `pendingDelete`

注意：

- 如果同一条本地便签有多次修改，只同步最新快照。
- 不需要把每次键入都作为单独操作上传。

### 8.3 pendingCreate

流程：

1. 调用 `POST /api/notes` 创建服务器便签。
2. 再调用 `PATCH /api/notes/:id` 写入完整内容。
3. 或新增支持带完整 payload 的创建接口。
4. 成功后保存服务器 `id`，状态改为 `synced`。

建议优化：

- 如果当前 `POST /api/notes` 只能创建空便签，M8.8 可先复用，再 PATCH。
- 后续再优化创建接口。

### 8.4 pendingUpdate

流程：

1. 调用 `PATCH /api/notes/:id`。
2. 成功后状态改为 `synced`。
3. 更新 `lastSyncedAt`。

### 8.5 pendingDelete

流程：

1. 调用 `DELETE /api/notes/:id`。
2. 成功后本地状态改为 `synced` 或从本地移除。
3. 服务器进入回收站。

## 9. 冲突策略

M8.8 采用简单策略：

```text
本机最后编辑优先
```

说明：

- 如果离线期间服务器版本也被修改，本机同步时覆盖服务器。
- 当前单用户为主，可接受。
- 后续多用户/多设备频繁使用后，再做冲突合并。

但必须记录：

```text
lastLocalEditAt
lastSyncedAt
```

为后续冲突检测留下基础。

## 10. UI 状态

### 10.1 全局离线状态

离线时显示轻量状态条或状态点：

```text
离线模式，修改已保存到本地
```

不要使用吓人的错误样式。

### 10.2 单条便签同步状态

可在保存状态处显示：

```text
已保存到本地
等待同步
同步中
同步失败
已同步
```

窄窗口下可用图标/点，但 tooltip 或状态详情要能说明。

### 10.3 同步失败

如果联网后同步失败：

- 不丢本地数据。
- 状态显示“同步失败，可重试”。
- 用户可手动点击重试。

### 10.4 防止误解的保存文案

在线和离线状态不要都显示同一个“已保存”，否则用户无法判断是否已经同步到服务器。

建议文案：

```text
已同步
已保存到本地
等待同步
同步中
同步失败
```

离线状态下输入后，应优先显示“已保存到本地”或“等待同步”，不要显示“已同步”。

## 11. 图片离线策略

本轮明确：

```text
离线不支持新增图片
```

行为：

1. 离线时图片上传按钮禁用。
2. tooltip/提示：

```text
离线模式下暂不支持上传图片
```

3. 已缓存图片可展示；未缓存图片显示占位。
4. 联网后图片功能恢复。

## 12. 安全与数据边界

1. 本地缓存只保存在当前浏览器/WebView 用户数据目录。
2. 不在 localStorage 存大体积便签数据，使用 IndexedDB。
3. 不明文保存密码。
4. 如果退出登录：
   - 清理当前用户本地缓存，或至少要求用户确认。
   - 未同步内容需要提示用户：

```text
还有未同步内容，退出登录可能导致本地草稿无法同步。
```

MVP 可先规定：

- 有未同步内容时禁止退出登录，或要求明确确认。

## 13. 验收清单

### 13.1 断网启动

1. 在线打开 App，确认有便签列表。
2. 断网。
3. 关闭 App。
4. 重新打开 App。
5. 不出现浏览器错误页。
6. 显示离线模式和本地便签。

### 13.2 离线新建普通便签

1. 断网。
2. 新建普通便签。
3. 输入标题和正文。
4. 关闭 App。
5. 断网重开。
6. 内容仍在。
7. 联网。
8. 自动同步到服务器。
9. 刷新网页端，能看到该便签。

### 13.3 离线新建待办

1. 断网。
2. 新建待办便签。
3. 添加 3 条待办。
4. 勾选 1 条。
5. 关闭 App。
6. 断网重开。
7. 待办和勾选状态仍在。
8. 联网后同步。

### 13.4 离线编辑已缓存便签

1. 在线打开已有便签。
2. 断网。
3. 修改内容。
4. 关闭 App。
5. 断网重开。
6. 修改仍在。
7. 联网后同步到服务器。

### 13.5 离线删除

1. 断网。
2. 删除一条已缓存便签。
3. 本地列表立即消失。
4. 联网。
5. 服务器端进入回收站。

### 13.6 图片离线降级

1. 断网。
2. 图片上传入口不可用或提示不支持。
3. 不影响文字/待办编辑。

### 13.7 同步失败保护

1. 模拟服务器不可用。
2. 本地编辑。
3. 恢复网络但服务器仍失败。
4. 显示同步失败。
5. 本地内容不丢。
6. 服务器恢复后可重试同步。

### 13.8 关闭重开专项

这是本轮最高优先级专项验收，必须单独录屏。

普通便签：

1. Windows App 在线打开一次，确保 App shell 已缓存。
2. 断网。
3. 完全退出 Windows App。
4. 重新打开 Windows App。
5. 新建普通便签。
6. 输入标题和正文。
7. 再次完全退出 Windows App。
8. 保持断网重新打开。
9. 新建便签仍在，标题和正文完整。

待办便签：

1. 断网。
2. 新建待办便签。
3. 添加 3 条待办。
4. 勾选其中 1 条。
5. 完全退出 Windows App。
6. 保持断网重新打开。
7. 3 条待办仍在，勾选状态仍在。

失败判定：

- 只要关闭重开后丢失任意离线输入，本轮不通过。
- 只要离线重开显示 WebView/浏览器默认错误页，本轮不通过。

### 13.9 联网同步专项

1. 承接 13.8 中离线新建的普通便签和待办便签。
2. 恢复网络。
3. 等待自动同步，不手动刷新。
4. 状态从“等待同步/同步中”变成“已同步”。
5. 打开线上网页端 `https://notes.hello-mylife.online`。
6. 离线创建的普通便签和待办便签都能看到。
7. 再次刷新网页端，内容仍存在。

失败判定：

- 本地显示已同步，但网页端看不到，视为不通过。
- 必须手动导出/手动复制才能同步，视为不通过。
- 同步后待办勾选状态丢失，视为不通过。

## 14. 技术部交付物

必须提供：

1. 实现说明：
   - 本地存储方案。
   - 数据结构。
   - 同步策略。
   - 冲突策略。
   - 图片离线降级策略。
2. Windows 壳录屏：
   - 断网启动不显示错误页。
   - 离线新建普通便签，关闭重开不丢。
   - 离线新建待办，关闭重开不丢。
   - 联网后自动同步。
3. Web 端验证：
   - 同步后线上网页可看到离线创建内容。
4. 构建：
   - `npm run build` 通过。
   - Windows 壳打包或至少壳内实机验证通过。

5. 离线专项验收证据：
   - Windows 实机断网启动录屏。
   - Windows 实机离线新建普通便签、退出、重开、不丢失录屏。
   - Windows 实机离线新建待办、退出、重开、不丢失录屏。
   - 恢复网络后自动同步到网页端录屏。

## 15. 不通过条件

出现以下任意一项，M8.8 不通过：

1. 断网打开 Windows App 仍显示 WebView/浏览器默认错误页。
2. 离线状态不能新建普通便签。
3. 离线状态不能新建待办便签。
4. 离线输入后关闭 App 再打开内容丢失。
5. 离线待办勾选状态关闭重开后丢失。
6. 联网后无法自动同步，需要用户手动复制内容。
7. UI 显示“已同步”，但服务器端实际没有数据。
8. 同步失败后本地内容丢失。
9. 图片离线不可用时没有明确提示，导致用户以为上传成功。

## 16. 建议实施顺序

建议技术部按以下顺序做，避免一次性把同步系统做复杂：

1. IndexedDB 本地数据层。
2. App shell 离线启动，不再出现 WebView 错误页。
3. 离线普通便签新建/编辑/关闭重开不丢。
4. 离线待办新建/编辑/关闭重开不丢。
5. 在线数据成功加载后写入本地缓存。
6. 联网后同步 `pendingCreate`。
7. 联网后同步 `pendingUpdate`。
8. 联网后同步 `pendingDelete`。
9. 同步状态 UI 和失败重试。
10. Windows 实机录屏验收。

其中 1-4 是本轮体验底线；5-10 是完整上线标准。

## 17. 通过标准

M8.8 通过标准：

> 断网时 App 仍能打开，用户可以新建/编辑文字和待办，内容自动保存到本地，关闭重开不丢；联网后自动同步到服务器。图片离线可以暂不支持，但必须清楚提示。

如果离线写入关闭后丢失，或断网启动仍显示 WebView 错误页，本轮不通过。
